# 2026-09-08 작업 기록

## TestMap 이안 상점 샘플

- 사용자 구현 요청에 따라 `QA_Marker_Vendor`의 기존 상점 연결에 이안 그림, 대화·구매·판매·회복 서비스·나가기 메뉴를 붙였다. 씬을 다시 만들지 않고 기존 상점 데이터와 마커 프리팹 참조를 유지했다.
- `ShopUI`는 FixedViewport 640×480, UI sorting layer/order 900으로 표시한다. 기존 대화창(998)이 위에 나오며, 대화 동안 상점 메뉴와 입력을 숨긴다. 목록은 5행 단위로 페이지가 바뀐다. 마우스/터치 클릭 경로는 추가하지 않았다.
- `ShopDefinition`에 이미지·상인명·대화 주제·회복 서비스를 추가했다. 짧은 대사는 현지화 키+기본 문구, 긴 대사/분기는 기존 `DialogueData` 참조를 지원한다. 구매/판매의 돈·인벤토리·구매 카운터 처리는 기존 거래 서비스를 사용한다.
- 최초 샘플에서는 `shop.ian.gender_reveal` 이벤트에 상점 배경 DOTween 흔들림을 연결했다. 후속 재사용 정리에서 공용 코드의 이안 전용 분기를 제거하고 상점별 `대사 이벤트 반응` 데이터로 옮겼다. 화면 흔들림 설정을 반영하고, 재실행/취소/종료 때 Tween과 위치를 복원한다.
- 무료 상담/마사지: 파티 전체 HP/AP 회복. `GlobalDataManager`와 `PlayerCharacter`의 회복·동기화·저장·로드 경계에서 장비 포함 최대치가 기본 최대치로 잘리지 않도록 보완했다. 전투 중 회복은 차단한다.
- `DialogueManager`는 대화 시작 프레임의 확인 입력을 무시해 메뉴 확인과 대사 넘김이 같은 입력에 함께 실행되지 않게 했다. 상점은 자신이 시작한 대화 generation만 취소하고, 다른 상태 소유자가 바뀐 경우 상태를 덮지 않는다.
- 신규 허브티(HP 30)/안정제(AP 10)를 카탈로그와 상점에 등록했다. 기존 QA 토닉과 상점 ID/GUID는 보존했다. QA 생성기는 초상화가 연결된 작성 상점을 덮어쓰지 않는다.
- 상세 파일과 제작 위치, 수동 확인 항목: [이안 상점 인계](yjlim/Patchnote/2026-09-08-ian-shop.md).
- 브랜치: `codex/gameplayEdit`. 커밋·푸시 없음. 아트 파일은 사용자 제공 원본을 그대로 참조한다.

### 검증 및 운영 메모

- UTF-8 유효성, GUID/카탈로그 참조, UI 좌표 경계와 수명주기 코드를 정적으로 검토했다. 실제 게임 화면과 플레이 확인은 수행하지 않았다.
- Runtime+Editor 정적 빌드 성공: 오류 0, 기존 경고 3 (`ConfigPanelUI` Inspector 필드 2개, Febucci `AnimatedLabelBase.settings` 1개). 명령: `dotnet build Assembly-CSharp-Editor.csproj --no-restore --nologo --verbosity minimal -m:1 -nodeReuse:false -p:BuildInParallel=false -p:UseSharedCompilation=false`. 변경 C# 6개가 생성된 csproj의 Compile 목록에 포함됨을 확인했다. `git diff --check` 통과.
- 후속으로 같은 씬에서 Battle 상태로 전환될 때 상점도 강제 종료하도록 보완했다. 최종 재컴파일 오류 0 / 출력 경고 2 (`ConfigPanelUI` 기존 필드), 13.49초. 패키지는 증분 빌드로 재컴파일하지 않았다.
- 독립 리뷰 후 비활성 ShopUI 재진입, 상품 없는 대화/회복 전용 상점, 직접 씬 변경의 상태 복원을 보완했다. `MapTransitionService`는 전환 이전 상태를 기록하기 전에 열린 상점을 닫아 상점의 임시 Cutscene 상태로 되돌아가지 않게 했다. 최종 변경 C#은 7개다.
- .NET 검증 첫 시도는 `Temp/obj/.../project.assets.json` 누락으로 `NETSDK1004`. 로컬 폴더를 restore source로 지정하고 `--disable-parallel -m:1 -nodeReuse:false`로 복원한 후 컴파일하는 방식으로 전환했다. Unity가 생성한 프로젝트나 패키지 소스는 수정하지 않는다.

### 하네스 교훈

- 2026-09-08 / Unity 생성 csproj 정적 확인: 기본 restore 병렬 실행에서 다수의 dotnet worker가 남고 quiet build 출력이 대기했다. 원격 피드를 쓰지 않는 로컬 restore와 `--disable-parallel -m:1 -nodeReuse:false`, build의 `-p:BuildInParallel=false -p:UseSharedCompilation=false` 조합으로 완료했다. 이번 restore 실행 시각(10:39:00~10:39:07)에 생성된 worker 110개만 정리했다. 일반적으로 모든 dotnet 프로세스를 일괄 종료하지 않는다. 검증: Runtime+Editor 빌드 종료 코드 0.
- 2026-09-08 / PowerShell의 rg 파일 선택: `ShopUI*.cs`를 경로 인자로 전달하면 Windows 경로 오류가 났다. `rg -g 'ShopUI*.cs' <검색할 디렉터리>`처럼 검색 디렉터리와 파일 패턴을 분리해 성공했다. 같은 방식으로 `Save*.cs` 등 여러 파일 패턴은 `-g`를 반복 지정한다. 검증: 대상 partial 파일 검색 결과 확인.
- 같은 파일을 apply_patch의 Delete File/Add File 두 작업으로 동시에 지정하면 multiple operations 오류가 난다. 기존 파일은 Update File의 문맥 기반 hunk로 수정한다. 검증: 변경 diff와 컴파일.
- PowerShell 보간 문자열에서 변수 바로 뒤에 콜론을 넣을 때는 `${taskName}:`처럼 경계를 표시한다. `$taskName:`은 변수 범위 문법으로 해석되어 ParserError가 발생했다. GUID 검증 명령을 해당 표기로 수정해 두 아이템의 상점·카탈로그 참조를 확인했다.
- 전역 지침의 `C:/Users/yjlim/.codex/memories` 경로는 이 호스트에 존재하지 않아 작업 공간의 이 기록에 남겼다. 관련 구현 인계는 위 패치노트에 연결했다.

## 후속: 이안 상점 입장 페이드와 음악

- `ShopDefinition`에 AudioClip 직접 참조와 입장 암전/등장/음악 전환 시간 필드를 추가하고, 기존 이안 상점 데이터에 `Content/Audio/AI/main_loop.wav`를 연결했다. GUID `3a042c53a6c9caf45b7e4048c36825d1`, AudioClip fileID 8300000. 기존 scene/prefab 연결은 그대로다.
- `ShopUI` 기존 FixedViewport Canvas 안에 재사용하는 콘텐츠 그룹과 검은 Image를 추가했다. 0.12초 암전 → 화면 교체 → 0.3초 밝아짐, DOTween unscaled, 입력 잠금/중복 입력 방지/강제 종료 cleanup을 연결했다. 입장마다 새 Canvas나 AudioSource를 만들지 않는다.
- 공용 AudioManager의 snapshot/crossfade/restore API를 사용한다. 입장 전 곡/위치/볼륨/무음 복구와 외부 음악 변경 시 복원 포기를 처리했다. 직접 씬 교체 시 아직 상점 곡인 경우만 중지해 새 씬으로 이전 곡을 되살리지 않는다.
- 약 15MB WAV의 원본은 유지하고 meta만 Streaming(`loadType: 2`), background load로 바꿨다. BGM 첫 로드의 동기 디코딩과 전체 디코딩 메모리를 줄이기 위한 설정이며 기기 성능은 측정하지 않았다.
- 수정 범위: ShopUI, ShopDefinition, QA_Shop_DebugInventory, main_loop.wav.meta, 이 기록과 기존 상점 패치노트. 사용자의 Silver SDF 및 다른 작업 파일은 그대로 두었다. 브랜치 `codex/gameplayEdit`, 커밋/푸시 없음.
- 검증: Runtime+Editor 컴파일 오류 0 / 기존 경고 3, UTF-8·diff·AudioClip GUID 참조 확인. 실제 Unity 실행/Play/Refresh/테스트는 하지 않았다. Temp가 다시 비워져 첫 빌드는 NETSDK1004였으며, 앞서 기록한 로컬 소스+단일 프로세스 restore 후 29.64초에 빌드 성공했다. 이후 확인 전에는 `Temp/obj/.../project.assets.json` 존재 여부부터 검사한다.

## 수정: 상점 입장 후에도 맵 환경음이 들리는 문제

- 처음에는 별도 환경음 채널 누락을 확인했으나, 씬 추적에서 실제 TestMap 원인을 특정했다. 루트 GameObject/fileID 234319518의 AudioSource 234319519가 피아노 곡 GUID `71e70314c58e28a43a4afa2fa3c4e100`을 믹서 없이 PlayOnAwake/Loop로 직접 재생했다. MapSettings의 BGM/환경음 참조는 비어 있었다. 실제 음향 재현 대신 사용자 보고와 직렬화·코드 호출 관계로 확인했다.
- TestMap의 해당 AudioSource 활성/PlayOnAwake만 0으로 바꾸고 같은 곡을 MapSettings 757403982의 `_mapBGM`에 연결했다. 기존 오브젝트·클립 참조·볼륨 원본 값은 삭제하지 않았다. 이제 공용 BGM 전환/복원/설정 볼륨을 사용한다. 열려 있는 씬은 재로드가 필요하다.
- AudioManager에 `SuppressAmbience()`를 추가했다. 환경음 source의 mute만 적용하고 원래 상태를 보존하며, 중복 핸들의 마지막 Dispose에서 복구한다. 기존 Play/StopAmbience의 클립 선택·fade는 유지되므로 새 맵의 환경음을 이전 맵으로 덮어쓰지 않는다.
- ShopUI는 입장 암전 시작 때 핸들을 받고 HideImmediate에서 해제한다. 정상 종료·강제 종료·비활성·파괴·씬 전환이 같은 해제 경로를 사용한다. BGM 없는 상점에도 적용하며 BGM·SFX·UI·음성 채널은 그대로다.
- 변경 파일: AudioManager.cs, ShopUI.cs, TestMap.unity, core.clinerules, 기존 상점 패치노트와 이 기록. 프리팹/음원 원본 변경 없음. 브랜치 `codex/gameplayEdit`, 커밋/푸시 없음.
- 검증: Runtime+Editor 컴파일 오류 0 / 기존 ConfigPanelUI 경고 2 (15.84초), UTF-8/diff 검사. 씬의 직접 AudioSource는 하나이며, 비활성화와 MapSettings의 동일 클립 GUID 연결을 확인했다. 실제 Unity 실행/Play/Refresh/테스트는 하지 않았다.
- 하네스 교훈(2026-09-08): 배경음 중복 신고에서 공용 AudioManager의 환경음 채널만 먼저 원인으로 판단한 접근은 TestMap의 실제 소스에 맞지 않았다. `_ambienceLoop`가 null이고 별도 AudioSource의 PlayOnAwake/Loop가 켜진 직렬화 증거로 확인했다. 이후에는 해당 씬/참조 프리팹의 모든 AudioSource·믹서 연결·클립을 먼저 대조하고 공용 채널 여부를 판단한다. 성공한 수정은 동일 곡을 MapSettings로 연결하고 우회 재생을 끈 것이다. 검증은 위 씬 diff/참조 검사까지이며 실제 음향 재현은 하지 않았다. 전역 메모리 경로가 없는 이 호스트에서는 이 프로젝트 기록을 사용한다.

## 후속: 다른 상점에서도 재사용하는 제작 구조 정리

- `ShopDefinition`에 `기본 대사 화자 (선택)`(`SpeakerData`), `서비스 메뉴 이름`, 주제별 `대화 중 상점 이미지 (선택)`, `대사 이벤트 반응` 목록을 추가했다. 반응은 이벤트 ID에 전체 상점 이미지와 흔들림 사용 여부·시간·강도를 연결한다.
- 짧은 대사는 상점 데이터에서 작성하고, 긴 대사·분기는 기존 대화 메이커의 `DialogueData`를 연결한다. 연결된 대화의 각 노드 화자·문구·`EventTriggerID`를 그대로 사용한다. 기본 화자 선택은 짧은 기본 대사에만 적용한다.
- `shop.ian.gender_reveal`는 공용 런타임의 특별 취급 없이 `QA_Shop_DebugInventory.asset` 반응 목록에만 유지한다. 다른 상점은 자신이 정한 이벤트 ID로 같은 기능을 사용할 수 있다.
- 상인 이미지와 주제/이벤트의 교체 이미지는 얼굴 초상이 아닌 전체 640×480 상점 화면용 그림이다. 얼굴 초상·목소리는 `SpeakerData`에서 설정한다. 대화 종료·중단 시 기본 상점 이미지를 복구하고 흔들림을 정리하며, 상점 종료 시 이벤트 구독도 해제한다.
- `ShopUI.cs`는 입력·세션·메뉴, `ShopUI.View.cs`는 UI 생성·배치, `ShopUI.Presentation.cs`는 이미지·이벤트 반응·암전·음악을 담당하도록 partial 파일로 나눴다. 기존 UI 배치 수치와 자식 오브젝트 이름은 유지한다.
- 제작 순서: 기존 상점 데이터 복제 → Shop ID·이름·전체 이미지·화자·상품·주제·서비스 수정 → `VendorMarker`의 상점 데이터에 연결. 복제 상품의 구매 카운터 Flag도 새 상점 전용으로 바꿔 한정 판매 횟수가 공유되지 않게 한다. 상세 항목은 기존 [이안 상점 인계](yjlim/Patchnote/2026-09-08-ian-shop.md)에 합쳤다.
- 변경 범위: `ShopDefinition.cs`, `ShopUI.cs`, 신규 `ShopUI.View.cs`/`ShopUI.Presentation.cs`와 meta, `QA_Shop_DebugInventory.asset`, 기존 작업 기록·패치노트. 기존 마커→상점 참조는 유지한다.
- 검증: UI 배치 메서드 5개의 이동 전후 내용 일치와 새 View GUID 중복 없음을 정적으로 확인했다. 이번 재사용 정리의 Runtime+Editor 정적 빌드 오류 0 / 기존 경고 3 (`ConfigPanelUI` 2개, Febucci 1개), `git diff --check` 통과. 신규 partial 파일은 `Temp/shop-cleanup.targets`로 이번 빌드에만 포함해 Unity csproj를 재생성하지 않았다. 실제 Unity Play·Refresh·플레이 테스트는 수행하지 않았다.

## 상점 후속 읽기 전용 검토

- 사용자 질문에 따라 추가 정리 후보만 점검했다. 대화창 scaled 닫기 Tween과 상점 unscaled 0.21초 재개 지연의 불일치, UI에 남은 회복 결제/환불 책임, 거래 후 Refresh 중복, 복제 상점의 구매 카운터 Flag 중복 경고 누락을 확인했다.
- 같은 BGM 클립을 외부에서 재요청하면 복구 소유권을 구별하지 못하는 제한도 후순위로 기록했다. 샘플에서 실제 발생했다고 단정하지 않았다.
- 상세 근거·줄번호·개선 범위: [상점 코드 검토](yjlim/feedback/2026-09-08-shop-code-review.md).
- 변경은 검토 문서와 이 기록뿐이다. 게임 코드·에셋 수정, Unity 실행/Play/Refresh/테스트, 빌드, 커밋·푸시는 하지 않았다. 브랜치 `codex/gameplayEdit`.

## 현재 구현 전반 읽기 전용 검토

- 사용자 요청이 상점에서 지금까지의 구현 전반으로 확대되어 전투/상태이상, 성장/장비/HP·AP, 맵/마커, 대화/UI, 제작 도구, 저장/오디오 주요 연결 경로를 정적으로 검토했다. 기준 커밋 `43297cfe`, 브랜치 `codex/gameplayEdit`. C# 622개는 검색 범위이며 전 파일 전 분기 감사나 실행 검증을 의미하지 않는다.
- 우선 수정 후보: POWER 조회 및 필드 피해의 장비 포함 최대치 누락, 기절·출혈의 실제 턴/행동 알림 누락, 사용 불가 장비의 순환 선택 정지, 방 복제의 OverworldEnemyMarker 조우 ID 미재발급, 적 스킬 완료 이벤트/AfterCurrentSkill 처리 누락.
- 별도 정리 후보: 대화 종료 시간 기준과 스킬/Room 전환 중단 처리, 상점 회복 거래 책임/Refresh 중복, 제작 검사 결과 연결과 번역 키 공유 안내, 옛 맵 제작 문서. 조건부 취약점과 현재 코드상 누락을 구분했다.
- 기존 3+3/도주 불가/Host, FixedViewport/Room 구조, 에디터 삭제·CSV 보호장치는 이번 검토에서 재작성 대상으로 선정하지 않았다. 발열/실기 성능 및 전체 저장 무결성은 검증하지 않았다.
- 상세 근거·발생 조건·최소 정리 방향: [구현 전반 코드 검토](yjlim/feedback/2026-09-08-project-code-review.md).
- 이번 변경은 검토 문서와 이 기록뿐이다. 게임 코드·에셋 수정, Unity 실행/Play/Refresh/테스트/빌드, 커밋·푸시는 하지 않았다. 작업 중 별도로 작성된 상점 검토와 기존 일일 기록은 보존했다.

## 검토 후 승인 범위 수정: 1·2·3·6·7·8

- 사용자 명시로 4번 방 복제 조우 ID와 5번 적 스킬 완료 시나리오 이벤트는 제외했다. 나머지 검토 항목을 기존 구조 안에서 수정했다. 브랜치 `codex/gameplayEdit`, 기준 `43297cfe`, 커밋·푸시 없음.
- 성장 조회는 현재 HP/AP를 변경하지 않고, 실제 성장 변경·필드 피해·저장·회복은 장비 포함 상한을 사용한다. 장비 순환 후보는 다른 동료가 점유한 수량을 반영한다.
- 기절 판정을 턴 감소 전에 보존하고 실제 행동 완료에 출혈을 연결했다. 출혈은 마지막 행동 후 만료하며 피해 뒤 후열/승패를 판정한다. 적 `skill.completed` 이벤트는 추가하지 않았다.
- 대사창은 unscaled 소유 Tween을 종료/재열기 때 정리하고, 상점은 고정시간 대신 실제 패널 닫힘을 기다린다. 시퀀스 취소는 Director부터 중첩 루틴 Dispose를 전파하며 블록 소유 Tween/잔상/투사체만 정리한다.
- 회복 결제를 Session/거래 서비스로 옮겼다. 회복 불가를 결제 전에 구분하고 실패 시 환불 결과를 확인한다. 저장 자원은 전체 계산 후 적용하며 씬 동기화 예외는 격리한다. 구매/판매/회복 후 중복 Refresh를 줄였고 같은 BGM 재요청도 버전으로 구분한다.
- 제작툴에 기존 ID·카탈로그 검사 결과 연결, 대사 복제 번역 키 공유 안내, 상점 구매 카운터 공유 경고를 추가했다. 옛 맵 제작 안내와 기존 제작 가이드를 현행화했다. 자동 Flag 재발급·데이터 마이그레이션은 하지 않는다.
- 변경 파일 묶음·책임·주의사항: [검토 후 정리 패치노트](yjlim/Patchnote/2026-09-08-review-cleanup.md). 기존 씬/프리팹/ScriptableObject/세이브 포맷은 변경하지 않는다.
- 중간 컴파일: Runtime·Editor 오류 0, 기존 ConfigPanelUI 경고 2. 신규 런타임1/Editor2 소스는 임시 MSBuild targets로 포함했고 생성 csproj/Unity는 건드리지 않았다. 실제 Play/Refresh/자동 테스트는 수행하지 않았다.
- 하네스 교훈: 기존 테스트 helper `SetPrivateField<T>`가 DialogueManager 전용인 것을 확인하지 않고 DialogueUI 검사에도 재사용해 첫 컴파일에 CS1503 2개가 났다. helper 대상 인자를 object로 바꾸고 실제 target.GetType()에서 field를 찾도록 수정한 뒤 재컴파일 성공. 다른 컴포넌트에 테스트 helper를 재사용할 때 대상 타입/조회 타입을 먼저 확인한다. 글로벌 yjlim 메모리 경로가 없는 이 호스트에서는 이 기록을 유지한다.
- Room 전환은 실행별 context와 overlay baseline scope를 보관하고 비활성/파괴/외부 씬 전환 시 한 번만 마감한다. fade-in 중 취소되어도 암전 이전 상태로 회수한다. 목적 방 commit 이후에는 이전 departure로 되돌리지 않고 새 핸들이 화면을 사용 중이면 기존 복구를 포기한다. 기존 Scene 전환 완료 경로는 유지한다.
- **최종 검증**: 방 전환을 포함한 Runtime·Editor 정적 빌드 오류 0 / 기존 ConfigPanelUI 경고 2, 8.34초. 신규 런타임 1개/Editor 2개는 `Temp/review-cleanup.targets`로 컴파일에 포함했고 검증 후 해당 임시 파일만 제거했다. 전체 C# 변경 42개는 기존 테스트 소스 보강도 포함한다. UTF-8, `git diff --check`, 신규 meta GUID 고유성, 패치노트 링크 확인. 씬/프리팹/기존 asset/csproj/sln 변경 없음, 제외한 방 복제 코드와 적 `PublishSkillCompletedScenarioEvent`/`AfterCurrentSkill` 호출 변경 없음.
- 독립 교차검토로 성장 장비 중복합산/재귀, 상점 회복 및 대사/BGM 소유권을 확인했다. 실제 Unity/Play/Refresh/테스트 실행 및 발열/성능 측정은 하지 않았다. broad GameState는 기존 enum 비교 정책을 유지하여 같은 Cutscene 상태를 서로 다른 연출이 이어받은 경우의 소유권까지 토큰으로 구분하는 확장은 하지 않았다.
