# 2026-06-15 업데이트

> 작업 단계: implement
> 목적: 전투 중 Game Module 전환 상태가 Action Sequence 여러 번에 걸쳐 유지되도록 런타임 아키텍처 보강

## [변경]

- YAML 편집기/라운드트립 UI 작업은 후순위로 밀고, 런타임 아키텍처 진행으로 우선순위를 되돌렸습니다.
- `IGameModuleActionRunner`에 `CurrentModuleId` 계약을 추가했습니다.
- `BattleManager`가 전투 시나리오 Action Context를 만들 때마다 새 `GameModuleActionRunner`를 만들지 않고, 전투 시작 시 생성한 battle-scoped runner를 재사용하도록 변경했습니다.
- `BattleScenarioActionContextFactory`는 runner의 `CurrentModuleId`가 있으면 `BattleScenarioData.OpeningModule`보다 이를 우선 사용합니다.
- 기본 Battle Game Module 등록을 `BattleGameModuleRegistryFactory.CreateDefault()`로 옮겼습니다. 현재 등록 모듈은 compatibility `turn_qte`입니다.
- `BattleScenarioRuntime.SessionState`와 `BattleSessionState`를 추가했습니다.
  - 현재 범위: scenario id, Primary Mode, opening module, current module
  - `IGameModuleStateStore`를 통해 `GameModuleActionRunner`가 current module을 battle-scoped state에 반영합니다.
- `IBattleSessionStateReader`를 추가하고 `BattleScenarioActionContextFactory`가 이를 `ActionExecutionContext` service로 등록하게 했습니다.
  - runtime actions / Game Modules는 scenario/module state를 읽기 위해 `BattleManager`에 직접 접근하지 않고 context service를 사용할 수 있습니다.
- `BattleParticipantSnapshot`을 추가하고 `IBattleSessionStateReader`가 참가자 스냅샷 목록과 `TryGetParticipant(...)`를 제공하게 했습니다.
  - 현재 범위: subject id, player/enemy 구분, 표시명, HP/MP, 생존 여부, bind/stun/berserk/defend/invincible 플래그
  - `BattleManager`는 전투 시작 직후, battle scenario Action Context 생성 직전, HP/MP 이벤트 브리지 호출 시점에 `_playerParty` / `_enemies`에서 읽기 전용 스냅샷을 갱신합니다.
- `IBattleParticipantCommandRunner`를 추가하고 battle scenario Action Context service로 등록했습니다.
  - 현재 명령 범위: pure damage, HP heal, MP heal, MP consume
  - 첫 구현체는 `BattleManager` 내부 adapter이며, 기존 `CharacterBase` HP/MP 변경, UI 이벤트, scenario HP 이벤트, participant snapshot refresh를 그대로 경유합니다.
- `battle.participant.*` Action Adapter를 추가했습니다.
  - `battle.participant.damage`
  - `battle.participant.heal_hp`
  - `battle.participant.heal_mp`
  - `battle.participant.consume_mp`
  - 공통 파라미터: `subject`, 1 이상의 정수 `amount`
  - Battle 기본 `ActionDirector` registry에 등록했습니다.
- `IGameModuleRuntime.Enter` / `Exit` / `Start`가 raw `ActionExecutionContext` 대신 `GameModuleRuntimeContext`를 받도록 변경했습니다.
  - 포함 정보: 원본 Action Context, 이전 모듈 ID, 대상 모듈 ID, `IBattleSessionStateReader`, `IBattleParticipantCommandRunner`
  - 향후 `aim_shooter`, `boxing`, `bullet_hell` 같은 concrete Game Module이 `BattleManager.Instance`를 직접 보지 않고 전투 세션 상태 조회와 HP/MP 변경 요청을 할 수 있게 했습니다.
- `BattleSessionState`에 battle-scoped flag 저장소를 추가했습니다.
  - 읽기: `IBattleSessionStateReader.Flags`, `HasFlag(...)`, `TryGetFlagValue(...)`
  - 쓰기: `IBattleSessionFlagStore.SetFlag(...)`, `ClearFlag(...)`
  - `GameModuleRuntimeContext.BattleFlags`로 concrete Game Module에도 전달됩니다.
- 전투 시나리오 Action으로 `battle.flag.set` / `battle.flag.clear`를 추가하고, Battle 기본 `ActionDirector` registry에 등록했습니다.
  - `battle.flag.set`: `flag` 필수, `value` 선택, 생략 시 `"true"`
  - `battle.flag.clear`: `flag` 필수
- Game Module 완료/결과 이벤트 통로를 추가했습니다.
  - `BattleEventData.GameModuleCompleted(moduleId, outcomeId, timing)` factory 추가
  - `BattleEventRuleData.OutcomeId` 추가
  - `BattleEventRuleEvaluator`가 `BattleEventType.GameModuleCompleted`를 module id + optional outcome id로 평가
  - `BattleScenarioRuntime.PublishGameModuleCompleted(...)` 추가
  - `BattleScenarioExecutionGate`가 `IGameModuleEventSink`를 구현하고 trigger queue/gate로 연결
  - `GameModuleRuntimeContext.ModuleEvents`로 concrete module이 완료 결과를 보고할 수 있게 함
- Scenario Source sync가 Game Module outcome rule을 보존하도록 보강했습니다.
  - `ScenarioSourceRuleDocument.OutcomeId` 추가
  - `ScenarioSourceImporter` / `ScenarioSourceExporter`가 `OutcomeId`를 복사
  - `ScenarioSourceYamlWriter`가 `GameModuleCompleted` 규칙을 `event: module.completed`, `module`, `outcome` 형태로 출력
  - `ScenarioAuthoringWindow` 규칙 요약에 모듈 결과를 표시
- QTE 전투 모듈화 작업을 시작했습니다.
  - `BattleTurnQteGameModuleRuntime`에 `IBattleTurnQteModuleController` 주입 seam을 추가했습니다.
  - `BattleGameModuleRegistryFactory.CreateDefault(...)`가 QTE controller를 받을 수 있게 했습니다.
  - `BattleManager`가 기본 `turn_qte` 모듈 등록 시 `BattleTurnQteModuleController` adapter를 주입합니다.
  - 심리스 전투/전용 배틀 씬 전투 시작 후 직접 `BattleState.TurnCalc`로 넘어가던 부분을 `IGameModuleActionRunner.Start(openingModule)` 경유로 바꿨습니다.
  - `turn_qte` start lifecycle은 현재 기존 `BattleManager`의 턴 계산 루프를 시작합니다. 아직 턴 계산/플레이어 선택/적 행동/QTE 실행 전체가 module class로 이동한 것은 아닙니다.
- QTE 전투 입력 게이트를 추가했습니다.
  - `OnPlayerActionSelected`, `OnSubMenuActionSelected`, `CancelActionSelection`, `CancelTargetSelection`, `ConfirmTargetAndExecute`는 이제 `turn_qte`가 활성 Game Module일 때만 처리합니다.
  - 다른 Game Module이 활성화된 상태에서 기존 Battle UI 콜백이 QTE 공격/스킬/아이템 실행으로 새어 들어가는 것을 막기 위한 1차 안전장치입니다.
- QTE 전투 모듈화 경로를 더 깊게 연결했습니다.
  - `BattleState.TurnCalc` / `BattleState.EnemyAction` 상태 진입은 이제 `IBattleTurnQteModuleController.RunTurnCalculation()` / `RunEnemyAction()`을 경유합니다.
  - `AdvanceTurn`, player turn begin, enemy turn begin도 `IBattleTurnQteModuleController` 계약에 포함했습니다.
  - 플레이어 행동 선택, 하위 메뉴 선택, 취소, 타겟 확정, 액션 완료도 `IBattleTurnQteModuleController` 뒤로 들어갔습니다.
  - `AfterCurrentSkill` 같은 시나리오 타이밍에서 다른 Game Module로 전환된 뒤 기존 `EndAction()`이 QTE `AdvanceTurn()`을 계속 진행하지 않도록 inactive-module guard를 추가했습니다.
  - `turn_qte` 모듈을 `Exit`할 때 QTE 타이머와 보류 중인 QTE 액션/스킬/아이템 상태를 끊습니다. 캐릭터 위치/카메라 리셋은 전환 연출을 방해하지 않도록 정상 action completion 경로에만 남겼습니다.
- QTE 전투 Battle Module 본문을 controller-owned implementation으로 옮겼습니다.
  - `BattleTurnQteModuleController`가 턴 계산, 턴 진행, player/enemy turn begin, 플레이어 입력, 타겟 확정, 플레이어 공격/스킬/아이템 실행, 적 행동, 방어 QTE 판정, 액션 완료를 직접 수행합니다.
  - 기존 `BattleManager`의 `TurnCalcRoutine`, `EnemyActionRoutine`, `ExecuteAttack`, `ExecuteSkill`, `ExecuteItem`, `HandleTurnQte*` 중복 본문은 제거했습니다.
  - controller는 아직 `BattleManager` nested class입니다. serialized field, battle event bridge, PositionManager/Camera/UI helper, legacy `SkillData.ActionTimeline`을 안전하게 재사용하기 위한 선택입니다.
- 관련 테스트를 보강했습니다.
  - 현재 runner module ID가 opening module보다 우선되는지 검증
  - 기본 battle module registry가 `turn_qte`를 포함하는지 검증
  - module switch가 injected state store를 갱신하는지 검증
  - `BattleScenarioRuntime`이 scenario 또는 null scenario에서 `BattleSessionState`를 생성하는지 검증
  - battle session state reader가 Action Context에 등록되고 module id 해석에 쓰이는지 검증
  - battle participant snapshot 저장/교체/EnemyData.EnemyId 기반 subject id 해석을 검증
  - battle participant command runner가 Action Context에 등록되는지와 command result 계약을 검증
  - `battle.participant.*` adapter의 runner 호출, runner 누락 실패, amount 검증, runner 실패 전파를 검증
  - Game Module이 `GameModuleRuntimeContext`를 통해 battle session / participant command seam을 받는지 검증
  - Battle Session flag 저장/교체/해제, Action Context flag store 등록, `battle.flag.*` adapter 동작을 검증
  - `GameModuleCompleted` rule이 module/outcome을 매칭하고, timing flush 또는 immediate routing으로 sequence 실행되는지 검증
  - Scenario Source importer/exporter/YAML writer가 `module.completed` outcome rule을 보존하는지 검증

## [의도]

- 이전 구조에서는 각 scenario trigger batch가 새 Action Context를 만들 때 `GameModuleActionRunner`도 새로 만들어질 수 있었습니다.
- 그러면 한 시퀀스에서 `module.switch: aim_shooter`가 성공해도, 다음 시퀀스는 다시 `OpeningModule`인 `turn_qte`로 시작한 것처럼 보일 위험이 있었습니다.
- 이 변경으로 “QTE 전투 중 적 HP 50% 이하 -> 대사/페이드 -> 슈팅 모듈 전환 -> 이후 다른 이벤트도 슈팅 모듈 상태를 유지” 같은 흐름을 runtime contract 수준에서 지킬 수 있습니다.
- `BattleSessionState`는 아직 전체 HP/MP/상태이상/참가자 소유자가 아닙니다. 이번 단계에서는 전투 모듈 연속성만 먼저 명시해, 이후 공통 전투 상태를 옮길 안전한 위치를 만들었습니다.
- 조회 seam인 `IBattleSessionStateReader`를 먼저 열어두면, 다음 concrete Game Module이 `BattleManager.Instance`에 의존하지 않고 전투 세션 상태를 읽을 수 있습니다.
- 참가자 스냅샷은 기존 Character/BattleManager 상태의 읽기 전용 뷰입니다. HP/MP 변경 소유권은 아직 `CharacterBase`와 기존 전투 흐름에 남겨 안정성을 지키고, 새 모듈이 상태를 읽는 경로만 먼저 표준화했습니다.
- 명령 runner는 HP/MP 변경 소유권을 새 시스템으로 옮긴 것이 아니라, 미래 Game Module이 기존 변경 경로를 한 군데로 요청하게 만드는 Adapter입니다.
- 이번 단계에서 이 command seam이 실제 Action Sequence grammar로 노출됐습니다. 따라서 future scenario나 module 전환 sequence가 legacy `SkillData.ActionTimeline` 없이도 HP/MP 변경을 요청할 수 있습니다.
- Game Module Interface도 한 단계 깊어졌습니다. 이제 concrete module은 broad Action Context를 직접 해석하는 대신 `GameModuleRuntimeContext` 하나를 받으므로, 모듈 구현자가 상태 조회/명령 요청 위치를 덜 추측해도 됩니다.
- Battle Session Flag는 “2페이즈 진입”, “슈팅 모듈 해금”, “적이 QTE를 거부함”처럼 전투 안에서 모듈과 시퀀스가 공유해야 하지만 저장 복구 대상은 아닌 사실을 담기 위한 영역입니다.
- Game Module Outcome은 “슈팅 모듈 승리”, “권투 모듈 실패”, “미니게임 타임아웃”처럼 모듈 내부 결과를 다시 Battle Scenario Rule로 올려보내기 위한 통로입니다.
- 이 통로가 있어야 `aim_shooter` 같은 concrete module이 자체 게임 루프를 끝낸 뒤 `BattleManager`에 직접 분기를 추가하지 않고도, 전투 데이터의 `when/do` 규칙으로 후속 대사/페이드/모듈 전환/승리 처리를 이어갈 수 있습니다.
- QTE 모듈화는 가장 중요한 후속 축입니다. 새 모듈을 만들기 전에 기존 QTE 전투가 Game Module Runner에서 시작되는 구조로 바꿔야, 나중에 QTE/슈팅/권투가 같은 Primary Mode 안에서 자연스럽게 교체됩니다.
- 편집기보다 먼저 이 runtime contract를 고정해야, 이후 YAML/커스텀 에디터가 잘못된 모델 위에 만들어지는 일을 피할 수 있습니다.

## [검증]

- `dotnet build HubToHome.sln --no-restore` 통과
  - 기존 `System.Net.Http` / `System.IO.Compression` 버전 충돌 경고와 `PlayerController._defenseReactionLocked` 미사용 경고는 남아 있습니다.
- Unity MCP EditMode `BattleScenarioRuntimeTests` + `BattleScenarioActionContextFactoryTests` 23개 통과
  - 참가자 스냅샷 신규 테스트 3개와 battle participant command runner 등록/결과 테스트 포함
- 새 action adapter 추가 후 Unity MCP는 현재 인스턴스를 찾지 못해 추가 EditMode 실행을 하지 못했습니다.
- Unity MCP script validation 통과
  - `BattleScenarioRuntime.cs`: standard validation 오류/경고 0개
  - `BattleScenarioActionContextFactory.cs`: standard validation 오류/경고 0개
  - `BattleManager.cs`: basic validation 오류/경고 0개
- 새 action adapter 추가 후 검증
  - `dotnet build HubToHome.sln --no-restore` 통과
  - C# LSP diagnostics 통과: `BattleSkillTimelineActionAdapter.cs`, `ScenarioActionParameterReader.cs`, `ScenarioPresentationCommandAdapterTests.cs`, `BattleManager.cs`
  - 첫 빌드에서 신규 파일이 Unity-generated csproj에 아직 포함되지 않아 adapter 타입을 못 찾는 오류가 났고, 강제 Unity refresh 없이 기존 포함 파일인 `BattleSkillTimelineActionAdapter.cs`로 adapter들을 옮겨 해결했습니다.
- `GameModuleRuntimeContext` 추가 후 검증
  - `dotnet build HubToHome.sln --no-restore` 통과
  - C# LSP diagnostics 통과: `IGameModuleRuntime.cs`, `GameModuleActionRunner.cs`, `GameModuleActionRunnerTests.cs`
  - Unity MCP script validation은 현재 에디터 인스턴스를 찾지 못해 실행하지 못했습니다.
- battle flag seam / action 추가 후 검증
  - `dotnet build HubToHome.sln --no-restore` 통과
  - C# LSP diagnostics 통과: `BattleScenarioRuntime.cs`, `BattleScenarioActionContextFactory.cs`, `BattleSkillTimelineActionAdapter.cs`, `ScenarioPresentationCommandAdapterTests.cs`, `BattleScenarioRuntimeTests.cs`
  - Unity MCP script validation은 현재 에디터 인스턴스를 찾지 못해 실행하지 못했습니다.
- module outcome event seam 추가 후 검증
  - `dotnet build HubToHome.sln --no-restore` 통과
  - C# LSP diagnostics 통과: `BattleManager.cs`, `BattleEventData.cs`, `BattleEventRuleEvaluator.cs`, `BattleScenarioRuntime.cs`, `BattleScenarioExecutionGate.cs`, `BattleScenarioActionContextFactory.cs`, `IGameModuleRuntime.cs`, `BattleScenarioRuntimeTests.cs`, `BattleEventRuleEvaluatorTests.cs`, `BattleScenarioActionBridgeTests.cs`
  - Unity MCP script validation은 현재 `No Unity Editor instances found` 응답으로 실행하지 못했습니다.
- Scenario Source outcome 보존 추가 후 검증
  - `dotnet build HubToHome.sln --no-restore` 통과
  - C# LSP diagnostics 통과: `ScenarioSourceDocument.cs`, `ScenarioSourceImporter.cs`, `ScenarioSourceExporter.cs`, `ScenarioAuthoringWindow.cs`, `ScenarioSourceSyncTests.cs`
- QTE 전투 모듈화 시작 후 검증
  - `dotnet build HubToHome.sln --no-restore` 통과
  - C# LSP diagnostics 통과: `BattleManager.cs`, `GameModuleActionRunner.cs`, `GameModuleRegistry.cs`, `GameModuleActionRunnerTests.cs`
  - Unity MCP script validation은 현재 `No Unity Editor instances found` 응답으로 실행하지 못했습니다.
- QTE 전투 입력 게이트 추가 후 검증
  - `dotnet build HubToHome.sln --no-restore` 통과
  - C# LSP diagnostics 통과: `BattleManager.cs`
- QTE 전투 모듈화 경로 보강 후 검증
  - `dotnet build HubToHome.sln --no-restore` 통과
  - C# LSP diagnostics 통과: `BattleManager.cs`, `GameModuleActionRunner.cs`, `GameModuleActionRunnerTests.cs`
- QTE 전투 Battle Module 본문 이동 후 검증
  - `dotnet build HubToHome.sln --no-restore` 통과
  - C# LSP diagnostics 통과: `BattleManager.cs`
- `git diff --check` 통과
  - Git의 LF/CRLF 변환 안내만 출력되었습니다.
- C# LSP diagnostics 통과
  - `BattleManager.cs`
  - `BattleScenarioActionContextFactory.cs`
  - `IGameModuleActionRunner.cs`
  - `GameModuleRegistry.cs`
  - `BattleScenarioActionContextFactoryTests.cs`
  - `GameModuleActionRunnerTests.cs`
  - `ScenarioPresentationCommandAdapterTests.cs`
  - `BattleScenarioRuntime.cs`
  - `GameModuleActionRunner.cs`
  - `BattleScenarioRuntimeTests.cs`
  - `BattleScenarioActionContextFactory.cs`
  - 참가자 스냅샷 추가 후 `BattleScenarioRuntime.cs`, `BattleManager.cs`, `BattleScenarioRuntimeTests.cs`

## [위험 / 후속]

- 실제 `aim_shooter`, `boxing` 같은 concrete module은 최후순위로 미룹니다.
- `BattleTurnQteGameModuleRuntime`은 이제 QTE 턴/입력/플레이어 액션/적 행동/방어 QTE/액션 완료 본문을 `IBattleTurnQteModuleController` 뒤로 모았습니다.
- 다음 우선순위는 controller를 별도 파일/Module로 추출할 수 있도록 필요한 runtime context를 설계하는 것입니다. 단, serialized field / scene reference migration 위험 때문에 이번 단계에서는 nested class 형태를 유지했습니다.
- 입력 게이트는 기존 UI 콜백을 막는 수준입니다. UI 자체를 모듈별로 교체/숨김 처리하는 세부 정책은 아직 `BattleUIController` 쪽 추가 정리가 필요합니다.
- 다음 아키텍처 우선순위는 `Battle Session State`의 참가자 스냅샷과 `IBattleParticipantCommandRunner`를 실제 concrete Game Module 또는 Action Adapter에서 사용하게 만드는 것입니다.
- 그중 Action Adapter 사용 경로는 시작됐습니다. 다음은 concrete `IGameModuleRuntime`이 이 reader/command seam을 실제 gameplay loop에서 사용하는 흐름입니다.
- `GameModuleRuntimeContext`로 concrete module이 reader/command seam을 받을 계약은 생겼지만, 실제 `aim_shooter` / `boxing` module 구현은 아직 없습니다.
- `BattleSessionFlag`는 battle-scoped 임시 상태입니다. 플레이어가 전투 밖에서 다시 만나도 기억해야 하는 사실은 여전히 Encounter Memory / SaveData 쪽으로 보내야 합니다.
- `GameModuleCompleted`는 결과 보고 통로만 만든 상태입니다. 실제 `aim_shooter` / `boxing` / 미니게임 모듈이 어떤 `outcomeId`를 발행하는지는 각 모듈 contract와 Action Catalog/Scenario Source 문서에 추가해야 합니다.
- Unity Editor EditMode 전체 테스트는 이번 작업 단위에서는 실행하지 않았습니다. 변경한 `BattleScenarioRuntimeTests`, `BattleScenarioActionContextFactoryTests` 클래스 단위 검증으로 우선 확인했습니다.
