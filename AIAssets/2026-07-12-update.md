# 2026-07-12 Update

## 작업

- 현재 구현된 상위 게임 아키텍처를 사람용 시각 자료로 정리했다.
- `Primary Mode`, `Action Director`, `Game Module`, `Presentation Service`, `Scenario Source`, `Battle Session State`, `Encounter Memory`, 기존 시스템 어댑터의 책임과 연결을 한 화면에서 확인할 수 있게 했다.
- `OverworldScene` 첫 진입에 같은 씬 기반 지하철 시네마틱을 추가했다. 별도 Scene 없이 오프스테이지 `OverworldCinematicStage`, 전용 Cinemachine Camera, DOTween 기반 `CinematicShotAsset`, 독립 Action Sequence를 사용한다.
- 시퀀스 메이커가 `BattleScenarioData` 외에 독립 `ActionSequenceAsset`을 선택해 YAML 미리보기, 검증, 저장, 안전한 런타임 에셋 반영을 할 수 있게 확장됐다.
- `SceneLoader.SceneRevealCompleted`와 `SceneActionSequenceTrigger`를 연결했다. Stage는 전역 로더의 검은 화면 아래에서 준비되고, 화면이 밝아진 뒤에만 시퀀스가 시작된다.

## 이유

- 시퀀스를 만들기 전에 어떤 레이어가 언제 실행을 소유하고, 어떤 상태가 모듈 전환을 넘어 유지되는지 빠르게 확인할 기준이 필요했다.
- 짧은 오버월드 전환마다 Scene을 나누지 않고도 카메라, 대상 이동, 페이드, 이후 게임플레이 카메라 복귀를 안전하게 제작할 기준이 필요했다.

## 파일

- `AIAssets/yjlim/feedback/2026-07-12-architecture-layer-map.html`
- `Assets/_Game/Content/Scenarios/Source/Overworld/overworld_intro_subway.sequence.yaml`
- `Assets/_Game/Content/Scenarios/Runtime/Overworld/overworld_intro_subway.asset`
- `Assets/_Game/Content/Cinematics/Overworld/overworld_intro_subway_arrival.asset`
- `Assets/_Game/Content/Scenarios/ActionCatalogs/OverworldCinematicActionCatalog.asset`
- `Assets/_Game/Scenes/Overworld/OverworldScene.unity`
- `AIAssets/yjlim/feedback/2026-07-12-overworld-subway-cinematic.md`

## 검증

- 시각자료 최초 정리 시점에는 코드/Unity 에셋 변경이 없었다.
- 현재 `CONTEXT.md`, 시나리오 파이프라인 규칙, Action Director/Execution Gate/Game Module 런타임 코드 기준으로 구조를 대조했다.
- Unity MCP에서 생성기를 실행해 씬과 에셋을 저장했고, 콘솔 컴파일 오류가 없음을 확인했다.
- focused Unity EditMode tests 10/10 통과: standalone source sync, cinematic shot definition, scene action adapter, subway content source/catalog/runtime asset 계약.
- `dotnet build HubToHome.sln --no-restore` 성공. 기존 MCP 참조 충돌 경고와 PlayerController 경고만 남아 있다.
- Play Mode에서 `오버월드 지하철 인트로 재생 테스트`로 SceneLoader 재진입 경로를 실행했다. 지하철 전용 카메라 활성화, 기차 이동, Cutscene -> Exploration 복귀, 기본 카메라 복귀, Player/ZEV 최종 프레임을 캡처로 확인했다.

## 후속

- 시퀀스 메이커에 새 Action Sequence 생성과 Rule 생성 UI를 추가하면, 이 지도에서 보이는 제작 흐름을 에디터 안에서 처음부터 끝까지 완결할 수 있다.
- Title -> Intro의 실제 이름 입력 UI를 거쳐 저장 슬롯을 다시 로드하는 전체 사용자 흐름은 별도 수동 확인이 남아 있다. SceneLoader를 통한 Overworld 재진입과 완료 플래그 초기화 재생은 Play Mode 테스트 메뉴로 검증했다.

## Sequence Maker 전면 개편 설계

### 작업

- 현재 3패널 Sequence Maker를 일반인과 AI가 함께 쓰는 공식 작업공간으로 재구성하는 `002-sequence-maker-workbench` 요구사항을 확정했다.
- 통합 탐색, 세로 블록 흐름, 타입 입력, 안정적인 Block ID, 실행 위치 표시, 안전 미리보기, Play Mode 실제 실행, 선택 블록 준비 실행을 설계했다.
- 사람은 Runtime Asset을 직접 다듬고 명시적으로 YAML에 저장하며, AI는 YAML을 중심으로 작업하는 양방향 저작 흐름을 확정했다.
- Action Library와 Trigger Library를 카테고리별 YAML 원본과 생성 Unity 에셋으로 관리하는 방향을 확정했다.
- 고정 `BattleEventType` 규칙을 Scenario Event ID와 Trigger Condition 조합으로 확장하고, Action Sequence는 유한한 흐름을 조율하며 지속적인 게임플레이는 Game Module이 소유하도록 경계를 확정했다.

### 문서

- `docs/plans/2026-07-12-sequence-maker-workbench-design.md`
- `specs/002-sequence-maker-workbench/spec.md`
- `docs/adr/0004-sequence-maker-dual-authoring-surfaces.md`
- `docs/adr/0005-catalog-driven-triggers-and-finite-sequence-orchestration.md`
- `AIAssets/yjlim/feedback/2026-07-12-sequence-maker-workbench-design.md`
- `docs/plans/2026-07-12-sequence-maker-workbench-implementation.md`
- `specs/002-sequence-maker-workbench/plan.md`
- `specs/002-sequence-maker-workbench/tasks.md`

### 검증

- 기존 `ScenarioAuthoringWindow`, `ActionCatalogAsset`, `ActionSequenceAsset`, `ScenarioActionData`, `ActionDirector`, 고정 Battle Event Rule 구현과 설계 요구를 대조했다.
- 이번 단계는 승인된 설계와 요구사항 기록이며 Runtime 코드와 Unity 에셋은 변경하지 않았다.

### 구현 기준선

- `dotnet build HubToHome.sln --no-restore -v:minimal` 성공: 오류 0, 기존 MCP 참조의 `System.Net.Http` / `System.IO.Compression` 버전 충돌 경고 4개.
- Unity MCP EditMode 전체 기준선은 205개 중 202개 통과, 3개 실패다.
  - `BattleTurnQteModuleControllerServiceTests.ExecuteSkill_SkipsDisabledSkillBlocksInTurnQtePath`: 기존 `PositionManager` 오류 로그 미처리.
  - `PreemptiveAttackPrefabTests.PlayerBasePrefabHasPreemptiveAttackSettings`: 기존 `Player_Base.prefab` 검색 실패.
  - `ScenarioSequenceOdinEditorWindowTests.KnownAction_ToActionData_WritesTypedParameters`: 기존 Odin typed parameter 결과가 `{}`로 남는 실패.
- Unity MCP에서 현재 `ScenarioAuthoringWindow`가 `시퀀스 메이커` 제목, 1471x1140 크기로 열려 있고 선택 대상이 없는 상태를 확인했다.
- 위 3개 실패는 Sequence Maker Workbench 구현 전 기준선이며, 후속 검증에서는 새 회귀와 분리한다.

### Block ID 1차 구현

- `ScenarioActionData.BlockId`와 재귀 ID Module `ScenarioBlockIdentity`를 추가했다.
- 기존 고유 ID 보존, 빈 ID 생성, 중복 ID 복구, source/runtime copy 시 ID 보존, 사용자 복제 시 전체 하위 트리의 새 ID 생성을 한 경로로 통합했다.
- 기존 main Sequence Maker의 복제가 누락하던 `DesignerLabel`과 `Note`도 공통 deep clone을 통해 보존된다.
- Scenario importer/exporter/reimport와 standalone sync의 복제 경로가 Block ID와 기획 메모를 잃지 않게 변경됐다.
- Unity EditMode `ScenarioBlockIdentityTests` 4/4 통과.
- source sync/Odin focused run 35개 중 34개 통과, 실패 1개는 기준선과 같은 기존 Odin typed parameter `{}` 실패다.
- `dotnet build HubToHome.sln --no-restore -v:minimal` 성공: 오류 0, 기존 참조/PlayerController 경고 6개.

### 시퀀스 메타데이터와 YAML 왕복

- `ActionSequenceAsset.Contract`를 추가해 설명, 사용 시점, 제작 상태, 태그, 허용 Primary Mode를 한 데이터에 모았다.
- standalone과 Battle Scenario source가 새 메타데이터와 모든 중첩 Block ID를 deterministic YAML로 왕복한다.
- 구조 블록 `parallel`은 `blockId`, 기획 표시명, 메모, 비활성 상태와 `children`을 보존하는 확장형을 지원한다. 기존 바로 중첩 목록 YAML도 계속 읽는다.
- Block ID가 없는 legacy YAML은 sequence ID와 트리 경로 기반의 결정적인 ID를 생성한다. 같은 source를 반복 import해도 같은 ID가 만들어진다.
- Unity focused EditMode 38/38 통과: standalone sync, scenario sync, Block ID.
- 실제 오버월드 지하철과 ZEV clone vertical slice 7/7 통과.

## Sequence Input, Value Binding, Sequence Call

### 작업

- `ActionSequenceContractData.Inputs`에 재사용 시퀀스의 공개 입력 ID, 표시 이름, 설명, 타입, 필수 여부, 기본 JSON 값을 추가했다.
- YAML에서는 값 참조를 `${input.actor}` 형태로 읽고 쓰며, Runtime Asset에서는 `{"$bind":"input.actor"}` 구조로 보존한다.
- `ActionExecutionContext`에 부모 값을 읽을 수 있고 로컬 값으로 덮어쓸 수 있는 스코프를 추가했다. 반환 값은 복제해 부모 토큰이 외부에서 변경되지 않게 했다.
- `ActionDirector`가 어댑터 실행 직전에 모든 바인딩을 리터럴 JSON으로 해석한다. 기존 QTE, 대화, 카메라, 오디오 어댑터 API는 변경하지 않았다.
- `sequence.call` 어댑터, `IActionSequenceResolver`, 자식 입력 바인딩, 실패/취소 전달, 런타임 재귀 방어를 추가했다.
- `SequenceCallGraphValidator`가 저장 전 직접/간접 순환과 없는 대상 시퀀스를 Block ID 기준으로 보고한다.

### 검증

- Unity EditMode `ScenarioValueResolverTests`, `ActionSequenceSourceSyncTests`, `SequenceCallActionAdapterTests`, `SequenceCallGraphValidatorTests` 집중 실행 25/25 통과.
- Unity 컴파일 오류 없음. MCP 플러그인의 기존 disposed-client 로그만 확인됨.
- 기존 사용자 변경인 Marker prefab, `EditorBuildSettings.asset`, `.codex/`는 스테이징 대상에서 제외한다.

## Action Library 계약 확장

### 작업

- `ActionCatalogEntry`에 사용 설명, 블록 요약 템플릿, 하위 카테고리, 검색 태그/별칭, 요구 컨텍스트, 허용 Primary Mode, 미리보기/준비 정책, 폐기/대체 정보, 아이콘/색상 ID를 추가했다.
- `ActionCatalogParameter`에 에디터 컨트롤 ID, 빠른 편집 여부, 최소/최대값, 단위, 허용 값 소스, 선택 옵션, 플레이스홀더를 추가했다.
- 기존 카탈로그의 새 필드 누락은 경고로만 표시하고 실행 가능 상태를 유지한다. 잘못된 범위, 중복 파라미터, 지원하지 않는 값 소스, 자기 자신 대체, 준비 정책 없는 Safe Preview는 오류다.
- `ActionCatalogContractCopy`로 모든 새 필드를 한 번에 복사하고 Odin 호환 화면의 수동 복사 코드를 제거했다.

### 검증

- Unity EditMode `ActionCatalogValidationTests` 9/9 통과.

## Action Library YAML 파이프라인

### 작업

- 카테고리 단위 `*.actions.yaml`을 읽고 쓰는 `ActionLibrarySourceParser`와 `ActionLibrarySourceWriter`를 추가했다.
- `ResolvedActionLibrary`가 여러 원본과 호환 카탈로그를 하나의 정렬된 목록으로 병합하고, 중복 Action ID를 양쪽 파일 경로와 함께 보고한다.
- `ActionLibrarySourceSync.ApplyToAsset`이 임시 카탈로그를 먼저 검증하고 성공한 경우에만 생성 에셋의 ID, 설명, Action 목록, 원본 경로, source hash를 교체한다.
- 주석, 인라인 목록, 따옴표/이스케이프, 파라미터 범위와 값 소스, 미리보기/준비 정책을 결정적으로 왕복한다.

### 검증

- Unity EditMode `ActionLibrarySourceSyncTests` 7/7 통과.

## 공식 Action Library 발행

### 작업

- flow, dialogue, audio, screen, cinematic, camera, actor, module, battle, timeline의 10개 카테고리 YAML에 28개 Action 계약을 작성했다.
- `BattleScenarioActionRegistryFactory`로 BattleManager 내부의 등록 목록을 분리하고 Scene factory도 `CreateRegistry()`를 제공하도록 바꿨다.
- `ActionAdapterRegistry.GetRegisteredActionIds()`와 `ActionAdapterContractScanner`로 런타임 어댑터와 라이브러리 계약의 양방향 누락을 검증한다.
- `ProductionActionLibraryBuildCommand`가 전체 원본을 읽고 계약 검증 후 공식 `ActionLibrary.asset`을 생성한다.

### 검증

- Unity 메뉴에서 공식 Action Library 재빌드 성공.
- Unity EditMode Action Library/adapter consistency 집중 테스트 23/23 통과.
- 등록 어댑터 27개와 Director 구조 Action 1개가 공식 계약 28개와 일치한다.

## Scenario Event / Trigger Condition 기반

### 작업

- `BattleScenarioData.TriggerRules`를 기존 `Rules` 옆에 추가해 레거시 에셋을 유지하면서 새 규칙을 병렬 수용한다.
- `ScenarioEventData`가 안정적인 Event ID와 타입 보존 JSON payload를 전달한다.
- `ScenarioTriggerRuleData`가 명시적 timing, checkpoint, once scope, 비활성화, 대상 Sequence 및 입력 바인딩을 보존한다.
- 재귀 all/any/negate 조건 트리와 `TriggerConditionRegistry`를 추가했다.
- 초기 조건 어댑터로 값 비교, 숫자 비교, 임계치 하향 통과, 참가자, 모듈 outcome, Encounter Memory 만남 횟수, flag 상태를 구현했다.

### 검증

- Unity EditMode `ScenarioTriggerRuleTests` 12/12 통과.
- Unity 컴파일 오류 없음. 기존 MCP disposed-client 로그만 확인됨.
- 기존 `BattleEventRuleData`와 시나리오 에셋은 변경/마이그레이션하지 않았다.

## Trigger Library YAML 파이프라인

### 작업

- 전투 이벤트, 공통 조건, 진행 상태 조건을 분리한 카테고리 YAML 원본 3개를 추가했다.
- Event/Condition 계약에 사용 설명, 문장 템플릿, 검색 태그/별칭, 모드/컨텍스트, 아이콘/색상, 폐기 대체 정보와 타입 필드 UI 메타데이터를 추가했다.
- `TriggerLibrarySourceParser`, writer, resolver, validator, sync가 결정적 왕복과 source hash를 제공한다.
- 중복 Event/Condition ID, 잘못된 JSON 기본값, 필수 UI 메타데이터, 숫자 범위, 잘못된 대체 ID를 생성 전 검증한다.
- `TriggerConditionContractScanner`가 공식 조건 7개와 기본 런타임 evaluator 7개의 양방향 누락을 차단한다.
- Unity 메뉴 `HubToHome > 시나리오 > Trigger Library 다시 만들기`와 생성 `TriggerLibrary.asset`을 추가했다.

### 검증

- Unity EditMode parser/writer/sync 테스트 8/8 통과.
- 공식 원본과 런타임 계약 및 생성 에셋 hash 테스트 2/2 통과.
- 공식 Event 6개, Condition 7개가 생성 에셋에 반영됐다.

## Battle Event Rule 호환 통합

### 작업

- `BattleTriggerRuleCompatibilityMapper`가 기존 5종 Battle Event Rule을 안정적인 Event ID와 Condition 트리로 변환한다.
- `BattleScenarioRuleRunner`가 생성 시 레거시 규칙을 한 번만 매핑하고, 새 `TriggerRules`와 함께 하나의 `ScenarioTriggerEvaluator`로 실행한다.
- `BattleEventRuleEvaluator`는 기존 API를 유지하는 호환 facade가 됐다.
- `BattleScenarioEventRouter`는 raw Event 지연 대신 평가된 Trigger를 timing/checkpoint별로 보관한다.
- `EnemyDefeated`, `SkillCompleted`, 일반 `ScenarioEventData`, 이름 있는 checkpoint 발행/flush 경로를 추가했다.
- Session, Encounter Memory, Save once scope를 `IScenarioTriggerHistory`로 통합했다. Save scope는 외부에서 fired ID만 import/export하며 전투 중간 상태를 저장하지 않는다.
- Event payload를 사용하는 Trigger target input binding을 해석하고, 실행 직전에 대상 Sequence Input 계약으로 검증한다.

### 검증

- 신규 호환/once/timing/checkpoint/input binding 테스트 15/15 통과.
- 기존 Battle evaluator/runner/router/runtime/bridge/gate 테스트 42/42 통과.
- Action Bridge typed input 및 잘못된 입력 차단을 포함한 테스트 13/13 통과.
- 지연 Trigger는 실제 flush 전 Encounter Memory에 기록되지 않고, flush 전 중복 발생도 한 번만 dispatch됨을 추가 확인했다.

## 확장 Trigger Rule Scenario YAML 왕복

### 작업

- 기존 `rules.when.event` 문법은 그대로 `BattleEventRuleData`로 왕복한다.
- 같은 `rules:` 목록에 `when.eventId`, 확장 timing/once/checkpoint, 재귀 all/any/not Condition, `do.inputs`를 표현하는 확장 문법을 추가했다.
- `ScenarioSourceRuleKind`가 레거시/확장 규칙을 구분하고 importer가 각각 `Rules`와 `TriggerRules`로 복원한다.
- `ScenarioTriggerIdentity`가 Condition Node ID를 보존하며 누락/중복은 scenario/rule/tree path 기반 deterministic ID로 보정한다.
- Runtime Asset reimport가 확장 규칙과 Condition 트리를 deep copy한다.
- 저장 전 Event ID, checkpoint, Condition ID/JSON, 대상 Sequence, typed input 키와 binding 경로를 검증한다.

### 검증

- 확장 YAML 중첩 Condition, binding, 혼합 규칙, ID, reimport, validation 테스트 7/7 통과.
- 기존 `ScenarioSourceSyncTests`와 `BattleScenarioValidationTests` 40/40 통과.

## 관찰 가능한 실행 세션과 병렬 완료 규칙

### 작업

- `ActionExecutionSession`과 순서가 보존되는 `ActionExecutionEvent`를 추가해 세션, 시퀀스, 블록의 시작/완료/실패/취소/건너뜀을 한 경로로 관찰할 수 있게 했다.
- `ActionPlayRequest`가 전체 재생과 안정적인 Block ID 기준 시작 위치를 함께 표현한다.
- 일시정지, 재개, 정확히 한 블록 실행, 실행 중 취소, 현재 블록과 상태 조회를 추가했다.
- 중첩 `sequence.call`과 병렬 자식은 호출한 부모 Block ID를 실행 기록에 보존한다.
- `flow.parallel`에 `all`, `any`, `race` 완료 규칙과 남은 자식 취소/정리 동작을 추가했다. 기존 데이터는 파라미터가 없으면 `all`로 실행된다.
- 공식 Action Library YAML과 생성 에셋에 병렬 완료 규칙의 한국어 설명과 segmented 선택 파라미터를 반영했다.

### 검증

- Unity 컴파일 성공, 콘솔 오류 0개 확인.
- `ActionExecutionSessionTests` 15/15 통과: lifecycle, skip/fail, 선택 시작, pause/resume/step/cancel, 중첩 호출, 병렬 all/any/race, handle 상태 알림.
- 기존 `ActionDirectorTests`, `SequenceCallActionAdapterTests`, `BattleScenarioActionBridgeTests` 23/23 통과.
- Action Library parser/validation/runtime adapter 일치 테스트 23/23 통과.
- 기존 사용자 변경인 Marker prefab, `EditorBuildSettings.asset`, `.codex/`는 작업 범위에서 제외한다.

## 선택 블록 Preparation Run

### 작업

- 선택 블록 이전 상태를 빠르게 만드는 `PreparationRun`과 Action별 `IActionPreparationAdapter` 레지스트리를 추가했다.
- `ApplyFinalState`, `ExecuteIsolated`, `SkipPresentation`, `RequireInput`, `Unsupported` 정책을 실제 실행 규칙으로 연결했다.
- Safe Preview는 `PreviewActionExecutionContextFactory`가 만든 분리 context만 받는다. 기존 화면, BGM, Game Module production 서비스는 격리된 preview 서비스로 교체된다.
- 저장, 보상, Scene 전환, 외부 효과는 adapter 실행 전에 차단한다. 실패와 취소 시 준비된 상태를 역순으로 복원한다.
- `EditorPreviewStateScope`가 복원 콜백과 Unity Undo 그룹을 함께 관리해 Stage 카메라와 대상 Transform을 되돌린다.
- 선택지형 RequireInput Action은 `previewDefault`를 사용하거나 입력 요청 상태에서 멈추며, 값을 전달한 뒤 같은 coroutine을 이어서 실행한다.
- `flow.parallel`의 `all`은 모든 자식을 준비한다. `any`/`race`는 에디터가 승자를 추측하지 않도록 직접 자식 `previewWinner`가 필수다.
- 미리보기 중 Block ID를 생성하지 않는다. 누락/중복 ID는 원본 에셋을 바꾸지 않고 오류로 보고한다.
- 실제 `OverworldCinematicStage`가 Cinematic Shot의 대상 종료 위치와 카메라 종료 줌을 즉시 적용하고, Safe Preview 종료 시 원래 위치/카메라 상태를 복원한다.

### 검증

- Preparation 정책, 입력 대기/재개/강제 종료, side-effect 차단, 분리 context, production 서비스 격리, 중첩 호출, 순환 방지, 병렬 정책, 복원 순서, ID 불변성 테스트 통과.
- 실제 Overworld Cinematic Stage 최종 상태 적용/복원 통합 테스트 통과.
- Unity EditMode 집중 실행 30/30 통과, Unity 컴파일 오류 0개.
- 기존 Action Director, sequence.call, Cinematic Stage, Action Library, 오버월드 지하철 계약을 포함한 확장 회귀 실행 69/69 통과.
- 공식 Action Library에 `flow.parallel.previewWinner` 파라미터를 추가하고 생성 에셋을 재빌드했다.

## 재귀 편집 기록과 안전 YAML 저장

### 작업

- 안정적인 Block ID를 기준으로 추가, 이동, 복제, 삭제, 활성화, 파라미터 변경을 수행하는 `SequenceEditCommandStack`과 역연산 명령을 추가했다.
- 다중 명령 transaction, undo/redo, 저장 revision, 선택 Block 보존, 중첩 Action tree 이동과 실패 rollback을 지원한다.
- Battle Scenario와 독립 Action Sequence를 하나의 `SequenceSaveCoordinator`로 저장하도록 target adapter 경계를 추가했다.
- 저장 전에 export validation과 source hash 충돌을 확인하고, 같은 폴더 임시 파일에 기록한 뒤 exact readback 및 parser/catalog round-trip 검증을 수행한다.
- 원본 교체 직전 hash를 한 번 더 확인해 검증 중 외부 변경도 차단하고, 기존 파일은 `File.Replace`, 새 파일은 검증된 temp move로 반영한다.
- 모든 파일 작업이 끝난 뒤에만 scenario와 child sequence source metadata를 함께 갱신한다. 실패 시 원본과 metadata는 유지한다.

### 검증

- `SequenceEditCommandStackTests` 14/14 통과.
- `SequenceSaveCoordinatorTests` 14/14 통과. validation 실패, temp 손상, parser round-trip 실패, 외부 변경, 기준 hash 누락, 저장 중 재변경, write/replace 예외, explicit overwrite, Battle/Standalone metadata를 포함한다.
- Unity 컴파일 오류 0개. 기존 MCP disposed-client 로그만 확인됐다.
- 사용자 변경 Marker prefab, `EditorBuildSettings.asset`, `.codex/`는 작업 범위에서 제외했다.

## 공식 Sequence Maker 워크벤치 셸

### 작업

- `SequenceMakerWindow.uxml`, `SequenceMakerWindow.uss`, `SequenceMakerTheme`로 구조, 스타일, 동작을 분리한 공식 UI Toolkit 창을 추가했다.
- 창 내부 중복 제목을 제거하고 상단 command bar, 단일 편집 대상, breadcrumb/search, 좌측 탐색, 중앙 수직 block flow, 우측 inspector, Problems/실행 기록/YAML drawer, 하단 저장 상태 구조를 만들었다.
- Battle Scenario와 독립 Action Sequence를 같은 target field로 열며, Battle 선택 시 포함 시퀀스를 탐색하고 중앙 flow에서 재귀 블록을 선택할 수 있다.
- 공식 Action Library 이름/카테고리/설명/색상을 block과 inspector에 연결하고, 검색, compact/comfortable 밀도, 분할 폭과 drawer 상태를 제공한다.
- 검증은 실제 exporter/importer/catalog validation을 사용하고, 저장은 `SequenceSaveCoordinator`를 사용한다. 저장/검증/undo/redo 버튼은 실제 기능에 연결했으며 아직 runtime context가 없는 playback 버튼은 비활성화했다.
- 공식 메뉴를 새 `SequenceMakerWindow`로 전환하고 기존 `ScenarioAuthoringWindow`는 개발 메뉴 아래의 임시 호환 창으로 이동했다.

### 검증

- `SequenceMakerWorkspaceStateTests` 10/10 통과: target 상호 배제, Battle sequence 선택, Block 선택, dirty, drawer, density, layout preference와 clamp.
- Unity 컴파일 및 UXML/USS import 오류 0개, 공식 메뉴 실행 성공.
- 실제 화면 pixel QA는 Windows 잠금 화면 해제 후 최종 시각 검증 단계에서 다시 수행한다.

## 통합 탐색과 사용 위치 인덱스

### 작업

- `SequenceAssetIndex`가 프로젝트의 Battle Scenario, 시나리오 소유 시퀀스, 독립 시퀀스를 stable asset key와 사람이 읽는 요약으로 인덱싱한다.
- 정확한 ID, 한국어 이름, 설명, 태그, 에셋 경로 검색과 중복 Sequence ID 보고를 추가했다.
- `SequenceNavigatorHistory`가 최근 작업과 즐겨찾기를 ID가 아닌 stable asset key로 저장해 시퀀스 이름/ID 변경 뒤에도 유지한다.
- `SequenceUsageIndex`가 기존 Battle Rule, 확장 Trigger Rule, 중첩 Action tree의 `sequence.call`, Battle Scenario 소유 관계를 하나의 참조 그래프로 만든다.
- missing target, 잘못된 `sequence.call` JSON, rename/delete 영향 수와 영향을 받는 scenario/sequence를 계산한다.
- 왼쪽 탐색을 `SequenceNavigatorView`로 교체해 검색, 최근 작업, 즐겨찾기, 전투 흐름, 전체 시퀀스, 사용 위치, 깨진 참조를 실제 이동 동작에 연결했다.

### 검증

- `SequenceAssetIndexTests` 7/7 통과.
- `SequenceUsageIndexTests` 6/6 통과.
- 공식 Sequence Maker 메뉴 재실행 및 Unity 컴파일 오류 0개 확인.

## 세로 중첩 Block Flow와 표준 편집

### 작업

- `SequenceFlowProjection`이 재귀 Action 트리를 깊이, 부모, 형제, 표시 순서, 접힘, 검색 문맥, Block별 검증 상태로 투영한다.
- `SequenceFlowCanvas`가 순차/병렬 Block 카드와 삽입 rail을 렌더링하고, 안정적인 Block ID 기반 선택을 유지한다.
- 다중 선택, 키보드 이동, 드래그 이동, 복사/잘라내기/붙여넣기, 복제, 삭제, 활성화, 병렬 묶기, Action 교체를 command stack transaction으로 연결했다.
- Battle 소유 Sequence의 선택 구간을 별도 `ActionSequenceAsset`으로 추출하고 typed `sequence.call`로 교체하는 안전한 편집 명령을 추가했다.
- Catalog 이름, category 색상, parameter 요약, quick 값, note, 검증 badge, breakpoint/bookmark와 실행 상태 스타일을 Block 카드에 반영했다.
- 추출 Undo는 Battle 소유 관계와 원래 Block을 복원하지만 이미 생성된 `.asset`은 자동 삭제하지 않는다. 의도치 않은 에셋 삭제를 피하기 위한 보수적 정책이다.

### 검증

- `ActionBlockSummaryTests` 7/7, `SequenceFlowProjectionTests` 7/7, 확장 `SequenceEditCommandStackTests` 19/19 통과.
- `SequenceFlowCanvasTests` 3/3 통과: 200개 Block과 201개 삽입 rail 생성 0.14초, 복사/붙여넣기 ID 독립성, 부모/자식 중복 선택 삭제 안전성을 확인했다.
- `Assembly-CSharp-Editor.csproj` 빌드 성공. Unity 컴파일 오류 0개 확인.
- 액션 추가는 현재 임시 catalog 메뉴를 사용한다. 다음 단계에서 검색, 호환성, 최근/즐겨찾기, 예시를 갖춘 전용 Action Picker로 교체한다.

## 검색 가능한 Action Library와 타입 인스펙터

### 작업

- 삽입 rail의 임시 메뉴를 `ActionPickerWindow`와 재사용 가능한 `ActionLibraryView`로 교체했다.
- 한국어 이름, Action ID, 설명, 사용 시점, 태그, 별칭, 예시, 파라미터 이름/설명을 다중 단어로 검색하고 전체 이름/ID 일치를 우선한다.
- 현재 Primary Mode와 알려진 runtime context를 기준으로 compatible, deprecated, unavailable을 구분한다. 호환용/중지/모드 불일치 액션도 이유와 함께 검색된다.
- 전체, 즐겨찾기, 최근, category 탐색과 프로젝트 사용 수, 상세 설명, 파라미터 목록, YAML 예시를 한 화면에 제공한다. 독립 메뉴 `HubToHome/시나리오/액션 라이브러리`도 추가했다.
- 인스펙터를 `ActionInspectorView`로 교체하고 Action 설명/사용 시점과 현재 Block의 이름, 메모, 활성 상태, 값 편집을 분리했다.
- `ParameterFieldFactory`가 string, number, duration, integer, bool, enum, color, vector2/3, actor/dialogue/audio/UI/module/animation/sequence 등 참조, list, JSON을 타입별 UI Toolkit control로 표시한다.
- `ValueSourceField`에서 literal과 input/event/session/memory/flag/context/result binding을 같은 자리에서 전환하며 구조화 `$bind` 토큰을 유지한다.
- Action 교체는 기존 Block ID를 유지한 채 Action ID와 기본 파라미터를 command stack transaction 하나로 바꾼다. 인스펙터의 모든 편집 후 dirty, YAML 미리보기, 검증 결과가 즉시 갱신된다.

### 검증

- `ActionPickerSearchTests` 7/7 통과: 전체 이름/ID 우선순위, 모든 metadata 검색, 다중 token, Primary Mode/context 호환성, deprecated/disabled 발견 가능성을 확인했다.
- `ParameterFieldFactoryTests` 20/20 통과: 16개 type/control 매핑, required/quick/range/unit, binding token, 참조 후보, enum/bool/color/vector control을 확인했다.
- `ActionPickerHistoryTests` 3/3 통과: 즐겨찾기 persistence/toggle, 최근 순서/중복 제거, 최대 12개 제한을 확인했다.
- Unity 메뉴에서 공식 시퀀스 메이커와 독립 Action Library를 열었고 C#/UXML/USS 오류 0개를 확인했다.
- Windows session이 잠금 화면이라 실제 픽셀 캡처는 차단됐다. 최종 시각 QA 단계에서 잠금 해제된 화면으로 다시 검증해야 한다.

## 공식 Sequence Maker 계약/Trigger Rule 편집

### 작업

- 시퀀스 이름, 설명, 사용 시점, lifecycle, 태그, 허용 Primary Mode, 요구 capability와 typed Sequence Input을 직접 편집하는 `SequenceInspectorView`를 추가했다.
- Sequence Input ID 변경은 계약과 재귀 Action tree의 정확한 `input.*` binding을 하나의 undoable command로 함께 갱신한다.
- 왼쪽에 native/legacy 이벤트 규칙 목록, 중앙에 한국어 `WHEN / IF / DO` 규칙 편집기, 오른쪽에 Event/Sequence 문맥 정보를 통합했다.
- Event와 Condition은 Trigger Library 계약을 사용하고, target Sequence Input은 같은 typed parameter/binding control을 사용한다.
- all/any 중첩 Condition tree 추가, 이동, 부정, 삭제와 legacy rule 변환을 `BattleScenarioEditCommandStack`으로 되돌릴 수 있게 했다.
- `TriggerRuleSimulator`가 payload/context/once 상태를 실제 `ScenarioTriggerEvaluator`에 넣고 조건별 match trace와 target input 해석 결과를 보여준다.
- Event, Condition, target Sequence는 한국어 이름, ID, 설명, category, tag, alias를 검색하는 `SequenceReferencePickerPopup`으로 선택한다.

### 검증

- formatter/simulator/Battle edit history/search/input rename 테스트를 추가했다.
- 외부 `Assembly-CSharp-Editor.csproj` 빌드 오류 0개.
- 최종 Unity Test Runner 실행은 Unity Editor 재연결 뒤 필요하다.

## Safe Preview, Live Test, 실행 기록

### 작업

- `SequencePlaybackController`가 안전 미리보기, Play Mode 실동작, pause/resume, step, stop, required preview input과 상태 전이를 소유한다.
- 전체 Safe Preview는 모든 Block의 Preparation 정책을 실행하고, 선택 Block Safe Preview는 선택 지점까지 준비한다.
- 선택 Block Live Test는 앞 Block을 실제 runtime context에서 빠르게 준비한 뒤 선택 Block부터 정상 `ActionDirector`로 실행한다.
- `IActionSequenceLiveContextSource`를 runtime seam으로 추가했다. `BattleManager`와 `SceneActionSequenceTrigger`가 첫 Adapter이며 editor Registry는 concrete type을 알지 않는다.
- Execution Session event와 Preparation step을 하나의 실행 기록으로 보여주고 stable Block ID를 누르면 해당 Block으로 이동한다.
- Problems drawer를 severity filter, metadata 검색, copy, stable object ID 이동이 가능한 `SequenceProblemsView`로 교체했다.

### 검증

- nested selected-prefix plan과 검색/복구 회귀 테스트를 추가했다.
- C# 전체 빌드 오류 0개. 기존 `PlayerController` obsolete/unused warning과 MCP package assembly warning만 남는다.

## 저장 충돌과 로컬 복구

### 작업

- `SequenceRecoveryStore`가 unsaved Runtime Asset YAML을 `Library/HubToHome/SequenceMakerRecovery`에 target GUID별 최대 5개 보관한다.
- 같은 내용은 중복 저장하지 않고, YAML content hash를 검증한 뒤에만 기존 Runtime Asset identity로 복구한다.
- 복구 capture는 0.75초 debounce하며 저장, target 전환, source reload, domain reload, 창 종료 전에 강제 flush한다.
- `SequenceConflictView`가 외부 변경 hash, 내 YAML, source reload, 명시적 overwrite, 파일 열기, recovery restore/delete를 제공한다.
- 성공한 validated atomic YAML save만 recovery를 지운다. 외부 YAML reload 전에는 recovery를 먼저 남긴다.

### 검증

- capture, deduplication, rotation, restore, tamper rejection, clear 테스트 6개를 추가했다.
- 기존 `SequenceSaveCoordinator`의 late-conflict 보호는 overwrite 승인 뒤에도 유지된다.

## 공식 편집기 단일화와 아키텍처 심화

### 작업

- 기존 `ScenarioAuthoringWindow`의 개발 메뉴를 제거하고 공식 UI Toolkit `SequenceMakerWindow`만 노출한다.
- Odin 시퀀스 구현은 과거 draft migration test용 코드만 유지하고 discoverable menu를 제거했다.
- ADR `0006-single-sequence-maker-and-recoverable-runtime-editing.md`에 공식 surface, recovery, Live Context Source, atomic input rename 결정을 기록했다.
- `improve-codebase-architecture` 리뷰의 Strong 항목 3개를 구현했다: concrete live lookup 제거, recovery I/O debounce, input rename/binding 원자화.
- 임시 리뷰 HTML: `C:/Users/Enou/AppData/Local/Temp/architecture-review-20260712-1432.html`.

### 검증 및 남은 게이트

- Unity EditMode 전체 `485/485` 통과. Sequence Maker 집중 테스트 98건도 별도 전부 통과했다.
- 공식 Sequence Maker를 Unity 메뉴에서 열어 무대상/탐색 상태의 레이아웃을 픽셀로 확인했다. 지원하지 않는 USS `:first-child`/`:last-child` 선택자 2개를 명시적 class로 교체했고 재오픈 후 관련 콘솔 오류는 0건이다.
- `SequenceFlowCanvasTests`에서 200 Block/201 insertion rail 생성과 stable Block identity 편집을 검증했다.
- `OverworldCinematicStagePreparationTests`와 `PreparationRunTests`에서 지하철 시퀀스 final-state 준비, 선택 Block 이전 빠른 준비, preview scope 복구를 검증했다.
- `ZEV_ArchitectureClone_TestScene` Play Mode Probe가 Encounter 시작, `turn_qte`, HP 50% Trigger, phase2 대사/페이드/BGM, `zev.clone.phase=shooter`, `aim_shooter` 전환을 모두 PASS했다.
- Play Mode 검증 중 phase2 Source에서 module transition 꼬리가 과거 시네마틱 확장 때 누락된 회귀를 발견했다. YAML에 `screen.fade`, `module.switch`, shooter 대사/BGM, `battle.flag.set`, `module.start`를 복원하고 공식 reimport 경로로 Runtime Asset을 동기화했다.
- `ZevScenarioCloneVerticalSliceTests`가 switch/flag/start/shooter dialogue 계약을 직접 확인하도록 강화했다.
- `duration: 0` actor move가 null Tween을 DOTween Sequence에 추가하던 경고를 제거했다. 같은 Play Mode 재실행에서 경고 없이 최종 Probe PASS를 확인했다.
- 작업 범위 scoped `git diff --check` 통과. Unity serializer가 빈 serialized string과 multiline scalar에 넣는 공백을 가진 generated Scenario/Catalog asset은 검사 대상에서 제외했다.
- `dotnet build Assembly-CSharp.csproj --no-restore`와 `Assembly-CSharp-Editor.csproj` 모두 오류 0개. MCP 의존성의 기존 assembly version 충돌 warning과 병렬 빌드 cache warning만 남는다.
- 사용자 변경 `Room_AreaMarker_AllGizmos.prefab`, `ProjectSettings/EditorBuildSettings.asset`, `.codex/`는 stage/commit 대상에서 제외한다.

## Sequence Maker 품질 보증 단계 시작

### 설계

- 실제 사용 흐름을 반복 실행하는 Editor Journey Harness와 Unity MCP 시각 탐색을 결합한다.
- 편집, target 전환, Undo/Redo, 저장, 외부 YAML 충돌, recovery, window/domain lifecycle, Safe Preview, Live Test를 연속 상태 전이로 검증한다.
- QA fixture는 기본적으로 `Library/HubToHome/SequenceMakerQA`와 메모리 안에 두고 기존 production scene은 저장하지 않는다.
- `SequenceMakerWindow`의 전역 edit history가 target 간 dirty/save 상태를 오염시키는지 최우선으로 재현한다.
- 재현 결과가 document policy 분산을 입증할 때만 `Sequence Maker Document Session`을 깊은 Module로 추출한다.

### 문서

- `docs/plans/2026-07-12-sequence-maker-quality-assurance-design.md`
- `docs/plans/2026-07-12-sequence-maker-quality-assurance-implementation.md`
- `specs/002-sequence-maker-workbench/tasks.md`에 Quality Hardening 체크리스트를 추가했다.

### Journey Harness baseline

- 실제 `SequenceMakerWindow.CreateGUI`와 target/render 경로를 실행하는 EditMode Journey 테스트 4개를 추가했다.
- 무대상, 독립 Sequence, Battle Sequence, Trigger Rule 선택 상태를 visible UI와 workspace 상태 양쪽에서 확인한다.
- Action Library의 `timer`, `sword` 같은 semantic icon ID를 Unity icon 이름으로 직접 조회해 Error 로그를 만들던 결함을 발견했다.
- `ActionBlockView`가 안전한 semantic-to-Unity 매핑과 기본 아이콘 fallback을 사용하도록 수정했다.
- Unity Journey 테스트 `4/4` 통과, 관련 icon Error 로그 0건을 확인했다.

## Sequence Maker 문서 상태와 입력 품질 강화

### 진단

- 실제 `SequenceMakerWindow` 여정에서 target A를 수정한 뒤 clean target B를 열어도 B가 dirty로 표시되는 결함을 RED 테스트로 재현했다.
- target A/B를 모두 수정하고 B만 저장했을 때 A history까지 저장 완료가 되는 결함을 실제 `Library/HubToHome/SequenceMakerQA` YAML 저장으로 재현했다.
- 원인은 Window가 열어 본 모든 Sequence/Battle history를 전역 합산하고, 저장 성공 뒤 모든 history에 `MarkSaved()`를 호출한 정책 누출이었다.
- 기존 `저장하지 않음` 선택은 Runtime Asset 변경을 되돌리지 않으므로 실제 폐기가 아니었다.
- 루트 trickle-down 단축키가 TextField의 native Undo/Redo보다 먼저 문서 history를 실행할 수 있었다.

### 작업

- `SequenceMakerDocumentSession`을 추가해 target별 Sequence/Battle history, 저장 checkpoint, recovery restore의 external dirty를 한 Module이 소유하게 했다.
- 독립 Sequence 저장은 현재 Sequence만, Battle 저장은 현재 Battle과 포함 Sequence만 저장 완료 처리한다. 다른 문서 history는 보존한다.
- recovery restore의 dirty 상태는 target별로 유지되어 다른 target을 열었다 돌아와도 사라지지 않는다.
- leave 선택을 `YAML 저장 후 이동`, `취소`, `저장하지 않고 이동`으로 명확히 구분하고 Runtime Asset/recovery 유지 사실을 안내한다.
- `SequenceMakerShortcutRouter`를 추가해 TextField 포커스에서는 native Undo/Redo를 보존하고, 캔버스에서는 문서 Undo/Redo, 모든 포커스에서 문서 Save를 처리한다.
- Window 재생성, 저장 실패, 외부 YAML 충돌, 명시적 overwrite 여정을 추가했다.

### 검증

- Document Session, shortcut, 실제 Window journey 집중 테스트 `23/23` 통과.
- Unity EditMode 전체 `512/512` 통과.
- 저장 fixture는 메모리 ScriptableObject와 `Library/HubToHome/SequenceMakerQA`만 사용하고 테스트 종료 시 삭제했다.
- 프로젝트 C# 컴파일 오류 0개. Coplay Unity MCP 패키지에서 refresh 중 `Cannot access a disposed object` 로그가 간헐적으로 발생하며 프로젝트 코드 오류와 분리해 후속 관찰한다.
- 사용자 변경 `Room_AreaMarker_AllGizmos.prefab`, `ProjectSettings/EditorBuildSettings.asset`, `.codex/`는 수정·stage하지 않았다.

## Sequence Maker 품질 강화 완료

### 추가 작업

- 저장 실패는 recovery/dirty를 유지하고 성공한 validated save만 현재 대상 recovery를 지우는 Window 여정을 추가했다.
- Recovery restore가 다른 TargetId/TargetType/AssetPath의 Runtime Asset에 적용되지 않도록 대상 신원 검증을 추가했다.
- 손상된 recovery metadata가 저장소 밖 `YamlFilePath`를 가리켜도 List/Restore/Delete가 따라가지 않도록 root confinement와 sibling YAML 규칙을 추가했다.
- 고정 random seed 100개에서 30회씩 insert, move, duplicate, delete, wrap parallel, parameter/label 변경을 실행하고 매 단계 Block ID와 selection invariant를 검사했다.
- 각 seed에서 전체 Undo canonical YAML이 원본과 같고 전체 Redo canonical YAML이 편집 결과와 같은지 확인했다.
- 1,000 Block Flow projection의 전체 lookup과 1초 editor budget을 검증했다.
- Safe Preview 실패/성공/중지/재시작과 EditMode Live Test fail-closed 상태 전이를 검증했다.
- 긴 Sequence ID의 전체 값을 read-only field tooltip에서 확인할 수 있게 했다.

### 최종 검증

- 실제 Unity Window 픽셀: 최소 `976x685`, 표준 `1500x950`, 와이드 `2100x1100`에서 겹침/버튼 잘림 없이 확인했다. 최소 폭의 navigator/flow 내부 스크롤은 의도한 fallback이다.
- Unity EditMode 전체 `522/522` 통과.
- `dotnet build Assembly-CSharp.csproj --no-restore`, `Assembly-CSharp-Editor.csproj --no-restore` 오류 0개. MCP 의존성의 기존 System assembly version warning만 남는다.
- clean `ZEV_ArchitectureClone_TestScene`을 저장 없이 Play Mode 실행해 encounter 전달, `turn_qte`, HP threshold, `aim_shooter`, `zev.clone.phase=shooter` 3단계 Probe PASS와 warning/error 0개를 확인하고 정지했다.
- Architecture Strong 항목인 Document Session 소유권과 Recovery Store confinement를 구현했다. Source Lifecycle Module 추출은 실제 정책 변화 시 진행할 `Worth exploring` 후보로 남겼다.
- 시각 아키텍처 보고서를 Chrome에서 열고 렌더를 검증했다: `C:/Users/Enou/AppData/Local/Temp/architecture-review-20260712-154820.html`.

## Sequence Maker 메뉴 정리와 Undo/Redo 표시

- Action/Trigger Library의 재생성 기능은 유지하되 사람용 Unity 메뉴는 제거했다. YAML 계약을 변경한 AI/파이프라인이 `Rebuild()` API를 호출한다.
- ZEV 복제 에셋 생성과 오버월드 지하철 생성/재생 테스트의 임시 sample 메뉴를 제거했다. Builder API와 테스트 코드는 필요한 자동화에서 직접 호출할 수 있다.
- `HubToHome > 시나리오`에는 공식 `시퀀스 메이커`와 `액션 라이브러리`만 노출한다.
- Undo/Redo 버튼의 문자 `U/R` fallback을 좌/우 화살표로 교체했다.
- Ctrl/Cmd+Z, Ctrl/Cmd+Y, Ctrl/Cmd+Shift+Z의 문서 Undo/Redo와 TextField native Undo/Redo 분리 계약을 유지했다.

## Sequence Maker 아이콘/텍스트 버튼 겹침 수정

- `검증`, `저장`, `액션` 버튼이 `Button.text` 자체 렌더링 위에 Image를 삽입해 같은 공간에서 겹치던 원인을 수정했다.
- 자식 Label과 background image 방식 모두 실제 Unity Button 렌더링에서 텍스트가 사라지거나 세로로 겹치는 것을 확인해 폐기했다.
- `검증`, `저장`, `액션`은 아이콘 없이 명확한 native text command로 표시한다.
- Undo/Redo와 playback처럼 기호가 더 명확한 icon-only 버튼만 아이콘을 유지한다.

## Sequence Maker 시퀀스 완전 삭제

### 작업

- Sequence Inspector 하단에 `위험 작업`과 `시퀀스 완전 삭제` 명령을 추가했다.
- `SequenceDeletionCoordinator`가 참조 분석, 복구 기록, YAML 저장/삭제, Runtime Asset 제거와 실패 복원을 한 Module로 소유한다.
- Trigger Rule, legacy Battle Rule, `sequence.call`, 다른 Battle 소유권, 동일 Battle 중복 소유가 있으면 삭제를 차단한다. 참조 인덱스나 Sequence ID/source path가 없어도 fail-closed로 차단한다.
- Battle 소유 Sequence는 recovery -> 목록 제거 -> 전체 Battle YAML 안전 저장 -> sub-asset 제거 순서로 처리한다. 저장 실패 시 정확한 원래 index로 되돌린다.
- 독립 Sequence는 export/round-trip과 source hash를 확인한 뒤 YAML과 Runtime Asset을 제거한다. Runtime 제거 실패 시 YAML과 `.meta` 원본 byte를 복원한다.
- 참조를 자동으로 수정하거나 연쇄 삭제하지 않는다. 확인창은 Sequence ID, YAML 경로, 제거 범위를 보여주고 취소를 기본 동작으로 둔다.
- Battle YAML 반영 뒤 sub-asset 제거만 실패한 부분 완료 상태는 낡은 선택을 유지하지 않고 index/workspace를 다시 읽는다.

### 검증

- 메모리 Adapter 테스트로 참조 차단, 저장 실패 롤백, source 충돌, source 삭제 실패, Runtime 삭제 실패 복원, 성공 순서를 검증했다.
- 실제 Unity AssetDatabase fixture에서 독립 YAML + Runtime Asset 삭제와 Battle YAML + Runtime sub-asset 삭제를 검증하고 fixture를 정리했다.
- 실제 Sequence Maker `1717x1100` 화면에서 위험 영역, 설명, 버튼 배치와 텍스트 겹침이 없음을 확인했다. 프로젝트 시퀀스 삭제는 실행하지 않았다.
- Unity EditMode 전체 `549/549` 통과.
- 사용자 작업 중인 Marker prefab, ZEV/Overworld Scenario Runtime Asset과 YAML, EditorBuildSettings, `.codex/`는 수정·stage하지 않는다.

### 파일

- `Assets/_Game/Scripts/Scenario/Editor/SequenceMaker/Documents/SequenceDeletionCoordinator.cs`
- `Assets/_Game/Scripts/Scenario/Editor/SequenceMaker/Inspector/SequenceInspectorView.cs`
- `Assets/_Game/Scripts/Scenario/Editor/SequenceMaker/SequenceMakerWindow.cs`
- `Assets/_Game/Scripts/Scenario/Editor/SequenceMaker/SequenceMakerWindow.uss`
- `Assets/_Game/Scripts/Scenario/Tests/Editor/SequenceDeletionCoordinatorTests.cs`
- `Assets/_Game/Scripts/Scenario/Tests/Editor/SequenceMakerWindowJourneyTests.cs`
