# 2026-06-17 업데이트

> 작업 단계: implement / verify
> 목적: 기존 ZEV 전투를 건드리지 않고 Scenario Source 기반 복제 vertical slice 검증

## [변경]

- 기존 `Enemy_ZEV.asset`, ZEV 스킬, battle scene, encounter wiring은 수정하지 않았습니다.
- 비교용 `Enemy_ZEV_ArchitectureClone.asset`을 새로 만들고 `EnemyId`를 `zev_architecture_clone`으로 분리했습니다.
- `zev_architecture_clone.scenario.yaml`을 추가했습니다.
  - `openingModule: turn_qte`
  - HP 50% 미만 이벤트 후 `aim_shooter`로 전환
  - `module.completed` victory outcome 이후 승리 시퀀스 실행
  - 대사, BGM, fade, module switch/start, battle flag, participant damage action을 사용합니다.
- `ZevArchitectureCloneSampleBuilder`를 추가했습니다.
  - 메뉴: `HubToHome/Scenario/Samples/Rebuild ZEV Architecture Clone`
  - 한국어 메뉴: `HubToHome/시나리오/샘플/ZEV 아키텍처 복제 에셋 재생성`
  - 샘플 dialogue asset, catalog asset, runtime `BattleScenarioData`, 복제 EnemyData를 생성/동기화합니다.
- `ZEV_ArchitectureClone_BattleScenario.asset`과 관련 dialogue/catalog runtime asset을 생성했습니다.
- `ZevScenarioCloneVerticalSliceTests`를 추가했습니다.
  - YAML import와 dialogue/audio 참조 해석
  - runtime asset과 catalog validation
  - 원본 ZEV와 복제 EnemyData 분리 여부를 검증합니다.
- `ScenarioSourceYamlParser`가 action parameter indentation을 더 유연하게 읽도록 수정했습니다.
  - 기존에는 action line보다 정확히 2칸 깊은 parameter만 읽었습니다.
  - 현재 문서화된 4-space YAML 예시처럼 더 깊은 indentation도 action parameter로 읽습니다.
- `ScenarioSourceRuntimeAssetReimportCommand`에서 새 sequence sub-asset 생성 시 `Undo.RegisterCreatedObjectUndo`를 제거했습니다.
  - recursive `ScenarioActionData.Children` 구조 때문에 Unity Undo가 serialization depth error를 내는 문제를 방지합니다.

## [의도]

- 이번 단계는 실제 ZEV 전투를 교체하는 작업이 아니라, 같은 재료를 복제한 새 sample battle scenario가 우리가 만든 상위 아키텍처를 탈 수 있는지 확인하는 작업입니다.
- 원본 ZEV와 복제 ZEV를 나란히 비교할 수 있어야 하므로, 기존 battle entry나 serialized reference는 건드리지 않았습니다.
- `zev_architecture_clone`은 향후 수동 연결 승인을 받은 뒤에만 encounter wiring에 붙일 수 있는 검증용 slice입니다.
- parser indentation 수정은 사람이 읽는 YAML 예시와 실제 parser 규칙이 어긋나는 문제를 해결한 것입니다.
- Undo 정책 수정은 runtime asset reimport가 에디터 콘솔을 더럽히지 않고 반복 실행될 수 있게 하기 위한 안전장치입니다.

## [검증]

- Unity MCP 메뉴 실행
  - `HubToHome/Scenario/Samples/Rebuild ZEV Architecture Clone`
  - 콘솔 에러 없이 `[ZEV Scenario Clone] 에셋 생성을 완료했습니다.` 확인
- Unity MCP `ZevScenarioCloneVerticalSliceTests`: 3개 통과
- Unity MCP `ScenarioSourceSyncTests`: 23개 통과
- `dotnet build HubToHome.sln --no-restore` 통과
  - 기존 Unity/MCP 관련 `System.Net.Http` / `System.IO.Compression` assembly version warning은 남아 있습니다.
- `git diff --check` 통과
  - LF/CRLF 변환 안내만 출력되었습니다.

## [위험 / 후속]

- Play Mode와 실제 scene 연결 검증은 아직 하지 않았습니다. 기존 ZEV 전투 비교를 위해서는 사람이 승인한 뒤 clone scenario를 별도 entry에 연결해야 합니다.
- `aim_shooter`는 아직 실제 슈팅 전투가 아니라 module shell과 pure combat session 중심입니다. `module.start: aim_shooter` 이후 실제 마우스 조준/UI/VFX 입력 루프는 후속 작업입니다.
- 샘플 ActionCatalog는 vertical slice용 최소 catalog입니다. production catalog가 생기면 샘플 catalog와 병합 또는 생성 규칙 정리가 필요합니다.
- generated runtime asset은 source YAML에서 재생성 가능한 결과물이지만, 현재는 비교와 에디터 확인을 위해 커밋 대상에 포함합니다.

---

## [추가 변경 - Clone Encounter 진입점]

- `OverworldEnemy`에 선택적 `BattleScenarioData _battleScenarioData` 필드를 추가했습니다.
  - 비워두면 기존처럼 BattleManager 기본 시나리오를 사용합니다.
  - 특정 접촉형 적만 Scenario Source 기반 battle flow를 쓰고 싶을 때 지정합니다.
- `DialogueEncounterContext`에 `BattleScenarioData` runtime field를 추가했습니다.
- `DialogueBattleNPC`에 선택적 `_fallbackBattleScenarioData` 필드를 추가했습니다.
  - 대화 선택 전투가 시작될 때 `DialogueManager -> BattleEncounterService.StartEncounter(..., battleScenarioData)`로 전달됩니다.
- `ZevArchitectureCloneSampleBuilder`가 `ZEV_Prefab.prefab`을 복제해 `ZEV_ArchitectureClone_Prefab.prefab`을 생성/갱신하도록 확장했습니다.
  - `EnemyCharacter.Data`는 `Enemy_ZEV_ArchitectureClone.asset`을 참조합니다.
  - `DialogueBattleNPC._fallbackEncounterEnemies[0]`도 clone EnemyData를 참조합니다.
  - `DialogueBattleNPC._fallbackBattleScenarioData`는 `ZEV_ArchitectureClone_BattleScenario.asset`을 참조합니다.
- 기존 `ZEV_Prefab.prefab`, 원본 ZEV enemy/skill asset, 기존 scene wiring은 수정하지 않았습니다.

## [추가 검증 - Clone Encounter 진입점]

- Unity MCP sample builder 메뉴 재실행 성공
  - clone prefab 생성 및 참조 갱신 확인
  - 콘솔 에러 없이 완료 로그 확인
- Unity MCP `ZevScenarioCloneVerticalSliceTests`: 4개 통과
  - 새 테스트가 clone prefab의 EnemyData / BattleScenarioData 참조를 검증합니다.
- Unity MCP `ScenarioSourceSyncTests`: 23개 통과

## [추가 위험 / 후속]

- `ZEV_ArchitectureClone_Prefab.prefab`은 비교용 실행 entry prefab입니다. 기존 overworld scene에 자동 배치하지 않았습니다.
- 실제 Play Mode 비교는 사람이 원하는 위치에 clone prefab을 배치하거나 별도 test scene/entry를 만든 뒤 진행해야 합니다.

---

## [추가 변경 - Scenario Editor Undo 안정화]

- `ScenarioAuthoringWindow`의 source save와 sequence row 편집 경로에서 `ActionSequenceAsset` 전체를 Unity Undo에 기록하지 않도록 정리했습니다.
- recursive `ScenarioActionData.Children` action tree는 Unity Undo serialization depth limit에 걸릴 수 있으므로, sequence 변경은 dirty marking으로만 처리하고 target scenario 수준의 Undo만 유지합니다.

## [추가 검증 - Undo 안정화]

- 콘솔을 비운 뒤 Unity MCP sample builder 재실행 성공
- Unity MCP `ZevScenarioCloneVerticalSliceTests`: 4개 통과
- Unity MCP `ScenarioSourceSyncTests`: 23개 통과
- 분리 실행 기준으로 `Serialization depth limit 10 exceeded at 'ScenarioActionData.Children'` 재발 없음

---

## [최종 변경 - Recursive Action 직렬화 안정화]

- `ScenarioActionData.Children`에 `[SerializeReference]`를 적용했습니다.
  - 이 필드는 `parallel` 같은 action tree가 다시 `ScenarioActionData` 목록을 갖는 재귀 구조입니다.
  - 값 직렬화로 두면 실제 순환이 없어도 Unity가 타입 구조를 깊게 따라가며 `Serialization depth limit 10 exceeded at 'ScenarioActionData.Children'` 에러를 낼 수 있습니다.
- `ScenarioSourceSyncTests`에 reflection 기반 회귀 테스트를 추가했습니다.
  - 앞으로 `Children`의 managed reference 직렬화가 빠지면 테스트가 실패합니다.
- `.agents/skills/hubtohome-scenario-authoring/SKILL.md`에 이 직렬화 규칙을 명시했습니다.
- `specs/001-scenario-authoring/tasks.md` Phase 7의 에디터 검증 대기 항목을 완료 처리했습니다.
  - 기존 scene wiring은 의도적으로 수정하지 않았습니다.
  - 검증은 clone YAML/runtime asset/prefab entry와 Unity MCP builder/test 경로에서 수행했습니다.

## [최종 검증]

- Unity MCP console clear 후 `HubToHome/Scenario/Samples/Rebuild ZEV Architecture Clone` 실행 성공
  - `[ZEV Scenario Clone] 에셋 생성을 완료했습니다.` 확인
- Unity MCP EditMode tests:
  - `ZevScenarioCloneVerticalSliceTests`: 4개 통과
  - `ScenarioSourceSyncTests`: 23개 통과
  - 합계 27/27 통과
- 같은 테스트 실행 후 `Serialization depth limit 10 exceeded at 'ScenarioActionData.Children'` 재발 없음
- 남은 Unity console warning:
  - `PlayerController._defenseReactionLocked` 미사용 warning 1건
  - 이번 Scenario Source / ZEV clone architecture 변경과 직접 관련 없는 기존 코드 warning입니다.

## [최종 위험 / 후속]

- `ZEV_ArchitectureClone_Prefab.prefab`은 비교용 entry prefab으로 준비되어 있지만, 기존 overworld/battle scene에는 자동 배치하지 않았습니다.
- 실제 Play Mode 비교는 사람이 clone prefab 배치 위치 또는 별도 test scene 방식을 결정한 뒤 진행하는 것이 안전합니다.
- `aim_shooter`는 module shell과 pure combat session 검증 단계입니다. 실제 마우스 조준, projectile, 전용 UI/VFX는 이후 gameplay slice로 구현해야 합니다.
