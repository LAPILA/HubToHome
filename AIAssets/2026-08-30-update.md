# 2026-08-30 빌드 UI·Chapter01 경로 조사

## 조사 범위

- 현재 `main`에서 제보된 Chapter01 Windmill 자산과 커밋 위치 확인
- Standalone 빌드의 Alt+Enter 전체화면 전환 설정 확인
- 빌드에 포함된 씬과 영상/시네마틱 진입 구조 확인

## 확인 결과

- 현재 `main` HEAD는 `2f7b514`이며 `Assets/_Game/Content/Maps/Regions/Chapter01/...` 경로는 존재하지 않는다.
- Windmill 씬/Room Prefab은 `origin/ART`와 `origin/codex/gameplayEdit`에 있는 `1bb306a`(`feat(chapter01): add Wizzel-ZEV staged encounter and stabilize interaction`)에서 추가되었다.
- 따라서 제보 경로가 “다른 이름으로 변경”된 것이라기보다, 현재 체크아웃한 `main`에 해당 기능 커밋이 포함되지 않은 상태로 판단된다.
- 현재 `ProjectSettings/EditorBuildSettings.asset`의 빌드 씬은 TestMap, Title, Intro, Prologue Subway Overworld, Battle, ShowcaseStation, TravelTrain, WideField이며 Windmill은 포함되지 않는다.
- `ProjectSettings/ProjectSettings.asset`은 `allowFullscreenSwitch: 1`, `fullscreenMode: 3`이다. 런타임 `GameConfigManager`도 전체화면 시 `Screen.SetResolution(..., FullScreenMode.FullScreenWindow)`를 호출한다.
- UI에는 640x480 기준 `CanvasScaler`와 `UIResolutionRefreshService`가 있으며, 화면 크기/전체화면 모드 변경 후 Canvas와 TMP 메시를 갱신한다. 다만 빌드 재현 화면이나 실제 플레이어 로그는 아직 확보하지 못했다.
- 프로젝트의 “영상 씬”은 동영상 파일/전용 씬이 아니라 `Cinematic Stage + Action Sequence` 구조다. 현재 확인 가능한 대표 진입은 `01_IntroScene` 이후 Build Settings에 등록된 `PrologueSubway/Scenes/OverworldScene`이며, 해당 맵의 상호작용/씬 공개 시점 트리거가 시네마틱 시퀀스를 재생한다.

## 빌드 확인 경로

- Unity 6 Editor의 Build Profiles/Build Settings에서 현재 씬 목록과 Windows 빌드 출력을 확인할 수 있다.
- 저장소에는 완성된 `.exe` 산출물이 추적되어 있지 않다.
- Unity MCP 패키지에는 `manage_build`의 `build`, `status`, `settings`, `scenes` 작업이 구현되어 있어 연결된 Editor가 있으면 빌드와 마지막 Build Report를 조회할 수 있다.

## 검증/제한

- `git status`: 조사 시작 시 clean.
- 씬을 열거나 저장하지 않았고, Unity Refresh/Play Mode/빌드를 실행하지 않았다.
- UI 파손의 정확한 원인은 빌드 실행 후 Alt+Enter 전후 스크린샷과 `Player.log`가 있어야 확정할 수 있다.

## 2차 확인: 브랜치 체크아웃 및 Windows 빌드

- `origin/codex/gameplayEdit`를 로컬 `codex/gameplayEdit`로 체크아웃했다.
- 첫 빌드는 브랜치 전환 직후 Unity Editor/Player 스크립트 레이아웃 불일치로 실패했다. `DialogueBattleNPC`와 `DialogueEncounterContext`의 직렬화 필드 차이가 로그에 기록됐다.
- Unity Asset Database refresh와 스크립트 재컴파일 완료 후 Windows 개발 빌드를 재시도했고 성공했다.
- 산출물: `Builds/Windows/HubToHome.exe`.
- 빌드 자동 실행과 수동 실행으로 중복 프로세스가 생겨 하나를 종료했으며, 현재 하나의 Player가 실행 중이다.
- 빌드/Unity가 Build Settings를 일시적으로 덮어쓴 것을 감지해 `ProjectSettings/EditorBuildSettings.asset`은 브랜치 HEAD의 Windmill 단일 씬 설정으로 복구했다.
- Alt+Enter 전후의 실제 육안 비교는 사용자가 실행 중인 Player에서 이어서 확인할 수 있다.

## 3차 확인: UI 정책과 Canvas A 첫 적용

- `docs/ui-policy.md`를 UI 정책 대표 문서로 추가했다. FixedViewport, WorldTracked, Fullscreen을 구분하고 640x480 FixedViewport에는 Expand를 사용하지 않는 원칙을 기록했다.
- `docs/plans/2026-08-30-ui-viewport-policy-and-overworld.md`에 단계별 구현 계획을 기록했다.
- TestMap 런타임에는 별도 UI Camera가 없고 `PPC` Game Camera만 존재하는 것을 Unity MCP로 확인했다. 첫 단계에서는 중복 카메라를 만들지 않고 Game Camera를 FixedViewport Canvas의 공통 출력 카메라로 재사용한다.
- `UIViewportService`와 `UIViewportServiceTests`를 추가하고, UIManager가 Overworld 패널을 FixedViewport로 등록하도록 연결했다. 내부 인벤토리/메뉴 계층과 직렬화 참조는 변경하지 않았다.
- `UIViewportServiceTests.ConfigureFixedViewportUsesSharedCameraAndNeverExpand` EditMode 테스트 1개가 통과했다.
- 변경 후 Windows 개발 빌드 `Builds/Windows/HubToHome.exe`가 성공했고 실행 중이다. 현재 캡처는 Battle 상태여서 Canvas A 오버월드 메뉴/인벤토리의 실제 화면 검증은 아직 완료하지 못했다.
- Unity 자동 생성/갱신으로 표시된 RenderSettings, TMP 리소스, ProjectSettings 변경은 이번 UI 코드 변경과 별개로 남아 있으며, 커밋 전에 내용과 원인을 재검토해야 한다.
