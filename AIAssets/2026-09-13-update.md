# 2026-09-13 작업 기록

## Z / X / C 전투 방어 구현

- 요청: Z 지속 가드·저스트 가드, X 회피, C 특수공격 연계 반격을 기존 전투에 구현한다. 이전 작업 트리의 부분 구현을 이어 완성하고 기존 정리 작업은 보존한다.
- 기준 브랜치 `codex/gameplayEdit`, HEAD `43297cfe`. 커밋·푸시 없음.
- 판정: 가드 배율 0.5 / 저스트 0.12초 / 회피 0.22초 / 반격 0.18초. 타격별 첫 입력 확정, X/C 실패 후 Z 보험 금지, 동시 입력 무효, 재입력의 Perfect 갱신 금지. 누르고 있던 Z의 일반 가드는 지속된다.
- 반격: 명시적 Counterable 스킬만 X/C 허용. C 성공 시 남은 적 스킬을 중단하고 현재 생존 전열 최대 3명이 각 ATK 배율로 반격한다. 기본 배율 1.5. 후열/사망자/추가 턴/AP/출혈 틱 제외.
- 제작/UI: 기존 방어 블록과 스킬 메이커를 확장하며 새 시나리오 Action은 만들지 않는다. C 공격 템플릿과 전조/시간/배율 검사, 입력별 안내와 기존 공격 QTE 레이아웃 복원을 연결한다.
- 변경 파일·사용 순서·수치·호환성·확인 항목: [Z/X/C 구현 인계](yjlim/Patchnote/2026-09-13-zxc-defense.md).
- 문서 계약: CONTEXT, battle.clinerules, 시나리오 스킬·Action Catalog Legacy Mapping을 현행화했다. 옛 시간제 방어 문서는 이력으로 유지하고 최신 인계 링크를 달았다.
- 입력 재설정 시 기본 Z/X/C fallback이 설정 입력과 중복되던 문제를 제거했다. QTE InputAction에 패드 South/East/North를 연결했으며 생성 inputactions 파일은 수정하지 않았다. 신규 모바일 터치 UI/패드 아이콘 자동 전환은 범위에 포함하지 않았다.
- QTE 열림·닫힘/결과 콜백의 재진입과 비활성 대상 취소를 보강했다. 방어/UI/반격 소유 Tween은 재활용 금지로 고정하고 자기 핸들만 해제한다. 공유 반격 서비스의 CancelActive를 모듈 해제/도주/종료/강제중단/파괴에 연결해 중단 코루틴의 finally에만 의존하지 않는다.
- 템플릿 최종 수치: 전조 0.6초, 판정 시간 0.8초, 반격 ATK×1.5, 원공격 ATK×1.8, 실패 추가 배율 1.0. 기존 적 자산이나 스킬 목록은 자동 변경하지 않는다. 소비되지 않은 방어 결과 뒤 다시 방어창이 나오는 제작 오류는 경고한다.
- **최종 정적 검증:** Runtime+Editor 컴파일 오류 0 / 기존 ConfigPanelUI CS0649 경고 2개, 8.17초. `dotnet build Assembly-CSharp-Editor.csproj --no-restore --nologo --verbosity quiet -m:1 -nodeReuse:false -p:BuildInParallel=false -p:UseSharedCompilation=false`. 신규 반격 서비스·테스트와 기존 작업의 신규 소스들이 생성 csproj에 이미 포함되어 있어 임시 targets나 csproj 변경은 필요하지 않았다.
- 전 단계 restore는 로컬 Temp source, `--disable-parallel -p:NuGetAudit=false -p:BuildInParallel=false -m:1 -nodeReuse:false`로 성공했다. 최종 `git diff --check` 통과. 기존 정리 작업과 함께 컴파일했으며 unrelated 변경은 되돌리지 않았다.
- Unity 실행/Play/Refresh/재임포트/씬 저장/테스트 실행은 하지 않는다. 기존 씬·프리팹·스킬 자산/세이브 형식 자동 변경, 이전 제외 범위인 방 복제 조우 ID·적 skill.completed 이벤트 수정은 하지 않는다.

## 재사용 교훈

- 2026-09-13 / Unity RequireComponent와 prefab 생성: 사용자 재생 후 Editor.log에 `Can't remove BunnySlimeCharacter ... OverworldEnemy ... depends on it`가 기록됐다. 생성기는 EnemyCharacter 파생 컴포넌트를 먼저 제거했지만 Unity가 의존성 때문에 삭제를 거절하고 예외 없이 진행하여 AI 둘이 저장됐다. 의존자부터 제거하고 실제 `GetComponents<EnemyCharacter>()` 결과를 검사한 뒤 공격 AI를 연결하도록 수정했다. 생성된 prefab은 대기 AI의 정확한 fileID(268549737344017875)만 제거하고 공격 AI/외부 루트 참조를 보존했다. 전후 직렬화 검사에서 passive1+attacker1(실패) → passive0+attacker1(통과). 실제 RequireComponent를 쓰는 회귀 테스트 소스를 추가했지만 Unity/테스트는 실행하지 않았다. 컴파일 성공은 prefab 생성 성공의 증거가 아니다.

- 2026-09-13 / 샘플 시나리오: Runtime 데이터와 같은 Maps 폴더에 YAML을 두는 첫 구성이 기존 `ScenarioSourcePathPolicy.RequireAbsolute`의 Scenarios 범위 검사와 충돌함을 정적으로 발견했다. 전역 정책을 느슨하게 하지 않고 원본을 `Content/Scenarios/Source/Battle/BunnySlimeBattleLab`로 이동해 정규화 검증 테스트 소스/정적 컴파일을 유지했다. 실제 importer/메뉴는 미실행. Runtime 생성 위치와 authoring source 위치는 다를 수 있다.
- 2026-09-13 / 타격 연출: `AttackAnimDelay`를 시작 지연으로 사용한 초기 구성은 실제 Execute가 애니메이션을 먼저 실행한 뒤 기다리므로 타격 시각이 맞지 않았다. 기존 필드 의미는 보존하고 샘플 전용 클립의 peak(.65/.85초)를 방어창 종료에 맞췄으며 해당 대기는 0으로 설정했다. 호출 순서/클립 키를 대조하고 정적 컴파일했으며 실기 체감은 아직 미검증이다.
- 2026-09-13 / 적 상태 샘플: 일반 적 스킬 실행기는 TargetType.AllyOnly를 읽지 않고 생존 플레이어를 대상으로 구성한다. 적 자기 보호막이라는 초기 조합을 독 시약으로 교체하고 보호막은 아군 스킬로 시연한다. 데이터 enum 존재를 모든 실행 경로의 지원으로 간주하지 않는다.
- 2026-09-13 / 병렬 작업 및 파일 탐색: 과거 에이전트가 남아 신규 스폰 제한에 걸린 경우 기존 에이전트에 독립 후속 작업을 배정했다. 타입 파일 경로를 추측한 읽기는 실패했으므로 `rg --files`/타입 선언 검색으로 실제 위치를 확인해 읽었다. PowerShell 외부 rg 인자에는 존재하지 않는 경로 glob을 넘기지 말고 디렉터리와 `-g`를 분리한다.

- 2026-09-13 / 테스트 소스 컴파일: NUnit의 `Does.Not.Contain(PlayerCharacter)`는 문자열 전용 overload로 해석되어 CS1503이 발생했다. 객체 컬렉션 검사는 `Has.No.Member(player)`로 바꾸고 Editor 컴파일 통과를 확인했다. 문자열 매처와 컬렉션 매처를 구분한다. 관련: `BattleLinkCounterServiceTests.cs`.
- 2026-09-13 / 메이커 템플릿: 실제 enum을 확인하지 않고 `TargetAreaType.Enemy`를 사용해 CS0117이 발생했다. 선언(`ItemData.cs`)의 `EnemyOnly`로 수정하고 재컴파일 성공했다. 이름이 비슷한 데이터 타입이라도 기존 enum 선언과 소비자의 의미를 먼저 확인한다. 관련: `SkillMakerWindow.cs`.
- 2026-09-13 / 도구 선택: 과거 종료된 에이전트 이름은 live 목록에서 안 보여도 재사용이 거부될 수 있다. 충돌한 `zxc_ui_authoring` 대신 현재 작업 고유 이름 `zxc_ui_finish`를 사용해 생성에 성공했다. 검색 패턴이 `---`처럼 대시로 시작하는 경우 `rg ... -- '패턴' 경로`로 옵션과 구분한다.
- 전역 yjlim 메모리 경로가 없는 이 호스트에서는 기존 운영 방식대로 프로젝트 일일 기록에 교훈을 유지한다. 인증 정보나 사용자 그룹 정보는 수집하지 않았다.

## BunnySlime 종합 전투 실험실

- 사용자 지정 원본 `tests_BunnySlime/tests_Enemy_BunnySlime.asset`은 유지하고 명시적 Editor 생성 메뉴 + fresh Play 전용 샘플을 구현했다.
- 샘플 데이터/순차 HP 페이즈 AI/공용 Host 씬 생성/640×480 메뉴/4가지 진입/실험 중 F8 중단/합격 대사와 보상. 세부 내용은 [인계](yjlim/Patchnote/2026-09-13-bunny-slime-lab.md), [사용 순서](../docs/bunny-slime-battle-lab.md).
- 기본 적 랜덤 선택은 `EnemyCharacter.SelectSkill`로 동일 이전했다. Lab만 결정론 선택을 override. `IEncounterDefeatPolicy`는 선택적으로만 패배 메뉴 복귀를 허용한다. 일반 전투 규칙 변경/자동 자산 마이그레이션은 하지 않았다.
- 카탈로그는 생성 명령에서 ID 충돌 검사 후 신규 항목만 등록한다. 기존 목록의 사본으로 검사해 중간 실패 시 실 목록이 반쯤 변경되지 않게 했다. 기존 생성 자산도 재작성하지 않는다.
- Source 경로 정책에 따라 YAML은 Scenarios/Source 아래, 실제 생성물은 Maps/Development 아래 둔다. 기존 importer/catalog validator와 source metadata를 사용한다.
- 정적 Runtime+Editor 빌드 오류 0 / 기존 ConfigPanelUI 경고 2개. 신규 소스가 아직 Unity csproj에 없으므로 일시적 `Temp/bunny-lab.targets`의 Compile Include로 검증했다. `.csproj`는 수정하지 않았다. 테스트 소스는 AI/페이즈 경계/Session 안전성/Source와 Action Catalog 계약을 포함하며 실행하지 않았다.
- Unity 실행/Play/Refresh/재임포트/생성기 실행/테스트 실행/기존 씬 저장 없음. 새 씬·데이터·Prefab·Animator는 메뉴 최초 실행 때 생성된다. 사용자 실기 확인은 남아 있다. 커밋·푸시 없음.
- 최종 증분 컴파일은 경고 0 / 오류 0, 10.45초. 앞선 전체 재컴파일에서 기존 ConfigPanelUI 경고 2개를 확인했다. 신규 코드/Source 22개 UTF-8 검증 및 샘플 meta GUID 중복 0, 원본 4개 자산 diff 없음, `git diff --check` 통과. 검증에만 사용한 `Temp/bunny-lab.targets`는 제거했다(생성 산출물/사용자 파일 삭제 없음).

### 후속: 토끼 공격이 실행되지 않음

- 사용자 실제 생성 결과가 이제 저장되어 있으며, Editor.log의 삭제 실패와 샘플 prefab의 중복 AI를 대조해 원인을 확정했다. 생성 과정에서 대기 AI가 앞에 남아 `GetComponent<EnemyCharacter>()`가 잘못된 AI를 고른 문제다.
- 변경: `BunnySlimeLabVisualBuilder` 의존성 순서/제거 결과/단일 AI 검사, `BunnySlimeLabContentBuilder` 기존 prefab 검증, `BunnySlimeBattleLabSession` 시작 전 AI 구성 검사, 생성된 `BunnySlime_Showcase.prefab`의 대기 컴포넌트 1개 제거. VisualBuilder 회귀 테스트 3개와 Session 중복 차단 테스트 추가.
- 스킬 7개 및 전용 타격 애니메이션 참조는 정상이라 다시 만들거나 수치를 바꾸지 않았다. 원본 공격하지 않는 토끼와 TestMap, 생성된 씬, 캐릭터/대사 데이터는 그대로다. 사용자는 같은 Lab 씬을 새 Play로 시작하면 된다.
- 실행 검증 대신 제공된 로그 재검토 + 직렬화 구조의 재현 전후 비교를 사용했다. 재생/강제 Refresh/재임포트/열린 씬 저장/커밋/푸시는 하지 않는다.
- 후속 최종 검증: 새 회귀 소스까지 포함한 Runtime+Editor 정적 컴파일 오류 0 / 기존 ConfigPanelUI 경고 2, 8.80초. 변경 소스/prefab UTF-8 유효, `git diff --check` 통과. 원본 토끼 데이터/prefab/TestMap 변경 없음. 브랜치 `codex/gameplayEdit`, 미커밋.
