# 2026-06-14 업데이트

> 작업 단계: Spec Kit 기준 `analyze`
> 목적: Action Director / Action Sequence 도입 전 기존 시스템 분석과 마이그레이션 방향 정리

## [문서화]

- Action Director 도입을 위한 기존 시스템 분석 HTML 문서를 추가했습니다.
  - `AIAssets/yjlim/feedback/action-sequence-migration-review-2026-06-14.html`
- `CONTEXT.md`에 런타임 아키텍처 용어를 추가했습니다.
  - `Primary Mode`
  - `Game Module`
  - `Action Sequence`
  - `Action Director`
  - `Action`
  - `Presentation Service`

## [분석 결론]

- 기존 전투/대화/UI/스킬 시스템을 즉시 갈아엎는 방식은 권장하지 않습니다.
- 새 `ActionDirector` / `ActionSequence` 계층을 먼저 만들고, 기존 시스템은 Adapter로 감싸서 현재처럼 동작하게 만드는 방향이 적합합니다.
- 이후 새 연출, 전투 모듈, 미니게임, 시네마틱 전환은 새 계층 위에서 작성하는 방식이 안전합니다.

## [유지/어댑터/신규 판단]

- 유지:
  - `BattleEncounterService`
  - `SceneLoader`
  - `MapTransitionService`
  - `DialogueData`
  - `UIManager` / `UIPanel`
  - `SkillData`
  - `CharacterBase` 계열
- Adapter:
  - `BattleManager`
  - `SkillActionBlock`
  - `QTEManager`
  - `BattleUIController`
  - `PlayerController`
  - 전투 카메라 컨트롤러
- 신규:
  - `ActionDirector`
  - `ActionSequenceAsset`
  - `ActionExecutionContext`
  - `ActorRegistry`
  - `UIRegistry`
  - `TargetResolver`
  - `PrimaryModeController`
  - `GameModuleRunner`

## [검증]

- 코드/씬/프리팹/에셋은 수정하지 않았습니다.
- Unity Editor PlayMode, 씬 저장, asset reimport는 수행하지 않았습니다.
- 분석은 `rg`, PowerShell 파일 스캔, 기존 `AIAssets`/`docs` 문서, 주요 C# 파일 정적 확인 기준으로 진행했습니다.

## [AI 협업 지침 정비]

- `AGENTS.md`에서 HubToHome와 무관한 DL 프로젝트 지침을 제거하고, HubToHome 전용 AI 협업 규칙으로 재작성했습니다.
- `README.md`를 프로젝트/AI 작업 시작점으로 정상화했습니다.
- `AIAssets/index.md`를 추가해 작업 전후 문서 운영 규칙과 업데이트 노트 양식을 고정했습니다.
- `docs/ai-collaboration-guidelines.md`를 추가해 사람과 AI가 함께 읽는 작업 라이프사이클, 충돌 방지 규칙, handoff 형식을 정리했습니다.
- `RuleFileforAI` 문서들을 현재 프로젝트 구조 기준으로 최신화했습니다.
  - `mainrule.clinerules`
  - `project-overview.md`
  - `codebase-reference.md`
  - `battle.clinerules`
  - `core.clinerules`
  - `overworld.clinerules`
  - `dialogue.clinerules`
  - `characters.clinerules`
  - `design-notes.md`
  - `milestone.md`

## [AI 협업 지침 의도]

- 두 개발자와 각자의 AI가 같은 문서 진입점을 사용하도록 정리했습니다.
- 작업 전 필수 읽기 순서, 코드 작성 시 주의할 Unity 직렬화 위험, 작업 후 기록 위치를 명확히 했습니다.
- 의미 있는 작업은 `AIAssets/YYYY-MM-DD-update.md`에 의도/변경/검증/위험을 남기도록 규칙화했습니다.
- 용어 변경은 `CONTEXT.md`, 시스템별 코드 규칙 변경은 `RuleFileforAI`, 설계/구현 계획은 `docs`에 남기도록 분리했습니다.
- 공유 `AGENTS.md`에서 개인 작업환경에 해당하는 Fork 사용 선호 문구를 제거했습니다.
- 개인 작업환경 규칙은 저장소 밖 상위 로컬 파일 `C:\Main\Unity\AGENTS.local.md`에 분리했습니다.
