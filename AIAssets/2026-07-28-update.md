# 2026-07-28 Update

## 토끼 슬라임 테스트 적

### 변경 사항

- `EnemyAction.Wait`를 기존 enum 끝에 추가하고 `EnemyCharacter.DecideAction()`을 가상 메서드로 열어 기존 직렬화 값과 일반 적 AI를 유지했다.
- `BunnySlimeCharacter`가 공격 대신 Wait만 선택하고 세 종류의 전투 문구를 순환하도록 구현했다.
- Turn QTE 모듈은 Wait 행동에서 공격 알림, 카메라 프레이밍, 방어 QTE, 피해를 실행하지 않고 내레이션 후 정상적으로 턴을 마친다.
- Aseprite 태그 `OverWorld`, `BattleIdle`, `Hit`를 각각 오버월드 Idle, 전투 Idle, `Hurt` 트리거에 연결했다. 원본 태그 이름 변경은 필요하지 않다.
- 전용 EnemyData, Animator Controller 2개, 전투/오버월드 Prefab을 생성하고 Runtime Content Catalog에 등록했다.
- TestMap의 `D_COMBAT_AND_COLLISION`에 `QA_BUNNY_SLIME_PASSIVE_ENCOUNTER`를 심리스 조우로 배치했다.
- TestMap 재생성 시 토끼 자산과 배치가 유지되도록 쇼케이스 빌더를 연결했고, 기존 Sublocation 생성기의 누락된 복귀 Spawn ID도 보완했다.

### 밸런스와 동작

- ID: `test.bunny_slime`
- 능력치: HP 35, ATK 0, DEF 150, SPD 4
- 즉시 처치 비활성화, 스킬/강공격/격앙 패턴 없음, EXP 5, Gold 0
- 사망은 기존 EnemyCharacter의 Hit 연출과 Fade 종료를 사용한다.

### 검증

- Unity C# 컴파일 성공.
- `BunnySlimeCharacterTests` 2개와 Wait 전투 서비스 테스트 1개 통과.
- 전투/오버월드 Prefab Missing Script 0건.
- Battle Controller 클립: `BattleIdle`, `Hit`; Overworld Controller 클립: `OverWorld`.
- Project Content Validation 오류·경고 0건.
- TestMap Play Mode 진입 5초 Smoke Test 오류·경고 0건.
- TestMap 인스턴스 부모와 위치 확인: `D_COMBAT_AND_COLLISION`, `(13.5, -8.4, 0)`.

### 남은 확인점

- 실제 플레이어 이동으로 접촉 및 F 선공을 각각 한 번 실행해 심리스 전투 왕복을 눈으로 확인한다.
- 껍질 파괴 전용 사망 애니메이션은 원본 아트에 해당 태그가 추가될 때 별도 연결한다. 현재는 기존 Fade 종료를 사용한다.
- 작업 중 보존한 TestMap 메모리 씬 백업은 `C:\tmp\HUBTOHOME_TestMap_BeforeBunny_20260728.unity`에 있다.

### 하네스 메모

- Windows 분할 쓰기 루트에서 `apply_patch`가 실패하면 원문 단일 일치 검증을 포함한 UTF-8 PowerShell 편집을 사용한다.
- Unity CLI의 한글 메뉴 경로는 JSON 문자열이 아니라 UTF-8 바이트 본문으로 전송해야 한다.
- 도메인 재로드 직전 `run_tests` 요청은 HTTP 잠금을 남길 수 있다. 컴파일 완료 후 실행하거나, 잠금 시 컴파일된 테스트 메서드를 Unity 도메인에서 직접 호출해 어설션을 확인한다.

## 개발 콘텐츠 폴더 및 임시 ZEV 정리

### 변경 사항

- `Assets/_Game/Content/Maps/Regions-TEST`를 `Assets/_Game/Content/Maps/Development/Regions`로 이동했다.
- 타이틀 전용 UI 스프라이트와 Intro 대사, 프롤로그·ShowcaseStation·TravelTrain 시네마틱을 각 개발 지역 폴더 아래로 모았다.
- `Content/Art/Samples`와 Area Marker 샘플은 `Maps/Development/Shared` 아래로 이동했다.
- 기차 해양 배경 파일은 참조 GUID를 유지하면서 `TrainOcean_Seafloor`, `TrainOcean_Midground`, `TrainOcean_Background`, `TrainOcean_Waves`로 이름을 변경했다.
- 프롤로그와 TestMap에 배치된 임시 ZEV 인스턴스, ZEV 비교 샘플, ZEV 기반 QA 마커를 제거했다. 정식 ZEV 아트·EnemyData·스킬·프리팹은 유지했다.
- TestMap의 기능 검증용 Enemy Marker는 일반 Slime 데이터로 교체하고 BunnySlime 심리스 조우는 유지했다.
- 비어 있던 ZEV 시나리오/대사 폴더와 사용되지 않는 빈 콘텐츠 폴더를 삭제했다.
- 개발 콘텐츠 경로를 `DevelopmentContentPaths`로 중앙화하고 빌더, 단축키, 테스트의 하드코딩 경로를 교체했다.
- 누락돼 있던 지역 씬 원본 `MapFieldStarter`를 `Development/Templates` 아래에 생성하고 모든 지역 빌더가 같은 템플릿을 사용하도록 복구했다.
- MapFieldStarter 생성기의 RoomDefinition/AreaDefinition 및 SequencePuzzleDefinition 저장 순서를 보강해 빈 ID와 Prefab 참조가 만들어지던 문제를 고쳤다.
- 템플릿의 오래된 ZEV 안내와 목적지가 없는 Sublocation 예시를 제거하고, House와 ForestPath 사이 ShortcutDoor에 전용 SpawnPoint와 실제 Room 이동 연결을 구성했다.
- Area Marker 샘플 생성기도 `Development/Shared/MarkerSamples`를 사용하게 해 삭제한 옛 샘플 폴더가 다시 생기지 않도록 했다.
- 생성만 되고 비어 있던 MapFieldStarter의 Materials 폴더를 제거했다.
- TestNPC 스케일 안내를 실제 임포트 설정인 32 PPU 기준으로 바로잡았다.

### 경로 정책

- 개발 월드: `Assets/_Game/Content/Maps/Development/Regions`
- 개발 공용 아트와 샘플: `Assets/_Game/Content/Maps/Development/Shared`
- 지역 씬 생성 템플릿: `Assets/_Game/Content/Maps/Development/Templates`
- 통합 QA 맵: `Assets/_Game/Content/Maps/Development/TestMap`
- 시나리오 YAML과 Runtime Asset은 소스 동기화 정책 때문에 기존 `Content/Scenarios/Source` 및 `Content/Scenarios/Runtime` 루트에 유지한다.
- 런타임 공용 Map Marker와 BattleScene은 개발 샘플이 아니므로 기존 위치에 유지한다.

### 검증

- Unity C# 컴파일 성공, Console 컴파일 오류 0건.
- SceneContract 12건, 프롤로그 시네마틱 5건, ShowcaseStation 데이터 빌더 9건, 지역 씬 빌더 7건 통과.
- 개발 씬 8개와 개발 프리팹 31개 Missing Script 0건.
- Project Content Validation 오류 0건. 기존 미지정 초상화·아이콘 경고 14건은 유지했다.
- 개발 씬·프리팹·데이터·문서의 임시 ZEV 문자열/자산 참조 0건, Build Settings의 모든 개발 씬 경로 갱신 확인.
- MapFieldStarter의 RoomDefinition 7개와 Area Marker 32개를 검사해 유효하지 않은 정의 및 구성 오류 0건을 확인했다.
- 최종 TestMap Play Mode 6초 스모크 테스트에서 오류 로그 0건을 다시 확인했다.

### 주의점

- `Assets/TextMesh Pro/Examples & Extras`의 두 SDF 자산 변경은 기존 사용자 변경으로 판단해 수정하지 않았다.
- 이번 작업은 커밋하지 않았다.
- PowerShell 단일 인용 문자열의 `` `r`n ``은 실제 개행이 아니라 문자 그대로 기록된다. 코드 치환의 개행은 here-string 또는 이중 인용 문자열로 만들고 저장 후 ``rg -n '`r`n'``으로 검사한다.
