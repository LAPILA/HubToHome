# 상점 제목 좌측 돌출 보정

상점은 공통 640x480 viewport를 사용하고 있었지만 제목의 고정 RectTransform이 화면 밖으로 생성되고 있었다.

- 기존: 중심 x `-205`, 폭 `330` → 좌측 경계 `-370`
- 수정: 중심 x `-145`, 폭 `330` → 좌측 경계 `-310`
- 결과: 640 기준 좌측 경계 `-320` 안쪽에 10픽셀 여백으로 배치

변경 대상은 `ShopUI.BuildView()`의 제목 좌표 하나이며, 상점 세션/목록/입력 동작은 건드리지 않았다.
